\documentclass[11pt,a4paper]{article}

\usepackage[margin=1in]{geometry}
\usepackage[T1]{fontenc}
\usepackage[utf8]{inputenc}
\usepackage{lmodern}
\usepackage{microtype}
\usepackage{amsmath,amssymb,amsfonts,amsthm}
\usepackage{booktabs}
\usepackage{array}
\usepackage{tabularx}
\usepackage{adjustbox}
\usepackage{enumitem}
\usepackage{graphicx}
\usepackage{hyperref}
\usepackage{xurl}
\usepackage{xcolor}

\hypersetup{
  colorlinks=true,
  linkcolor=blue!45!black,
  citecolor=blue!45!black,
  urlcolor=blue!45!black,
  pdftitle={Valid State Transition Framework},
  pdfauthor={Martin Petrasek}
}

\setlist[itemize]{leftmargin=1.4em}
\setlist[enumerate]{leftmargin=1.6em}
\renewcommand{\arraystretch}{1.15}
\emergencystretch=3em
\Urlmuskip=0mu plus 2mu
\graphicspath{{./}{/Users/mpetr/Desktop/}{/Users/mpetr/Desktop/VSTF_GitHub/}{/Users/mpetr/Desktop/R_Universe/vstf/}}

\newtheorem{definition}{Definition}
\newtheorem{principle}{Principle}
\newtheorem{observation}{Observation}
\newtheorem{criterion}{Criterion}
\newtheorem{hypothesis}{Hypothesis}
\newtheorem{conjecture}{Conjecture}
\newtheorem{limitation}{Limitation}
\newtheorem{assumption}{Assumption}
\newtheorem{lemma}{Lemma}

\newcommand{\VST}{\ensuremath{\mathrm{VST}}}
\newcommand{\VSTR}{\ensuremath{\mathrm{VSTR}}}
\newcommand{\VSTE}{\ensuremath{\mathrm{VSTE}}}
\newcommand{\VSTJ}{\ensuremath{\mathrm{VSTJ}}}
\newcommand{\VSTF}{\ensuremath{\mathrm{VSTF}}}
\newcommand{\VIRR}{\ensuremath{\mathrm{VIRR}}}
\newcommand{\OSD}{\ensuremath{\mathrm{OSD}}}
\newcommand{\PSI}{\ensuremath{\mathrm{PSI}}}
\newcommand{\IET}{\ensuremath{\mathrm{IET}}}
\newcommand{\E}{\mathbb{E}}
\newcommand{\R}{\mathbb{R}}
\newcommand{\1}{\mathbf{1}}
\newcommand{\dd}{\mathrm{d}}
\newcommand{\Texit}{T_{\mathrm{exit}}}
\newcommand{\Treturn}{T_{\mathrm{return}}}
\newcommand{\VSTFStepBox}[2]{%
  \fbox{\begin{minipage}{0.82\linewidth}
  \centering\textbf{#1}\\[-0.15em]\small #2
  \end{minipage}}%
}
\newcommand{\VSTFEventCascadeFigure}{%
  \begin{adjustbox}{max width=\linewidth}
  \begin{tabular}{c}
  \VSTFStepBox{Raw dynamic signal}{measured trajectory, sensor stream, or benchmark output}\\[0.55em]
  $\Downarrow$\\[-0.1em]
  \VSTFStepBox{Candidate event}{hysteresis-separated crossing or segmented transition attempt}\\[0.55em]
  $\Downarrow$\\[-0.1em]
  \VSTFStepBox{Measurement gates}{distinguishability, localization, retention, and domain admissibility}\\[0.55em]
  $\Downarrow$\\[-0.1em]
  \VSTFStepBox{Outcome status}{$S_k\in\{\mathrm{accepted},\mathrm{rejected},\mathrm{pending}\}$}\\[0.55em]
  $\Downarrow$\\[-0.1em]
  \VSTFStepBox{Claim boundary}{accepted VST count, rejected attempts, and complete denominator ledger}
  \end{tabular}
  \end{adjustbox}
}
\newcommand{\VSTFBar}[2]{\textcolor{#1}{\rule{#2}{0.75em}}}
\newcommand{\VSTFHMMBenchmarkFigure}{%
  \begin{adjustbox}{max width=\linewidth}
  \begin{tabular}{lcc}
  \toprule
  Event layer & System A & System B\\
  \midrule
  Raw activity & \VSTFBar{blue!60}{5.4em}\ 6.19M & \VSTFBar{green!55!black}{4.4em}\ 5.00M\\
  Crossings & \VSTFBar{blue!60}{4.8em}\ 2.34M & \VSTFBar{green!55!black}{1.0em}\ 0.47M\\
  Committed & \VSTFBar{blue!60}{4.0em}\ 1.56M & \VSTFBar{green!55!black}{1.0em}\ 0.39M\\
  Retained & \VSTFBar{blue!60}{1.2em}\ 105k & \VSTFBar{green!55!black}{2.5em}\ 217k\\
  Valid VST & \VSTFBar{blue!60}{0.9em}\ 75.7k & \VSTFBar{green!55!black}{2.3em}\ 191k\\
  \midrule
  \VSTE{} & 0.00197 & \textbf{0.00680}\\
  PPV & 0.616 & \textbf{0.872}\\
  Sensitivity & 0.373 & \textbf{0.843}\\
  \bottomrule
  \end{tabular}
  \end{adjustbox}
}
\newcommand{\VSTFRankFigure}{%
  \begin{adjustbox}{max width=\linewidth}
  \begin{tabular}{lcc}
  \toprule
  Baseline ranking metric & Discordance vs \VSTE{} & Visual diagnostic\\
  \midrule
  Raw activity per energy & 0.138 & \VSTFBar{orange!75!black}{1.2em}\\
  Crossing events per energy & 0.552 & \VSTFBar{red!70!black}{4.8em}\\
  Committed events per energy & 0.382 & \VSTFBar{red!55!black}{3.3em}\\
  Retained events per energy & 0.057 & \VSTFBar{green!55!black}{0.5em}\\
  Retention yield & 0.166 & \VSTFBar{orange!75!black}{1.4em}\\
  Validity yield & 0.554 & \VSTFBar{red!70!black}{4.8em}\\
  \bottomrule
  \end{tabular}
  \end{adjustbox}
}

\title{\textbf{Valid State Transition Framework}\\
\large A Pre-Specified Event-Level Adjudication Framework for Stabilized Outcomes}

\author{Martin Petrasek\\
Institute for Resonant Synthesis and Field Physics (IRSFP)\\
Prague, Czech Republic\\
\href{mailto:martin.petrasek@irsfp.org}{martin.petrasek@irsfp.org}\\
ORCID: 0009-0007-4512-9528}

\date{\today}

\begin{document}
\maketitle

\begin{abstract}
Many systems are evaluated by activity-like quantities: operations per watt, signal amplitude, entropy production, risk scores, biomarker shifts, perturbation response, or transient behavioral output. These measures are often indispensable, but they do not by themselves establish that a system has produced a durable, distinguishable, and operationally meaningful outcome. A processor may dissipate energy without producing an accepted stable result; a physiological system may show changing signals without clinically actionable state transition; a tumor model may lose viability without a retained shift away from a pathological tumor-state; and a neuromodulated motor system may display evoked activity without restored voluntary control.

This manuscript introduces the \emph{Valid State Transition Framework} (VSTF), a conservative event-level measurement grammar in which the elementary unit of analysis is not raw activity but a \emph{valid stabilized state transition}. A state-valid transition is defined as a segmented movement between operationally specified state regions that satisfies localization under an observation model, distinguishability under a declared decision rule, first-exit or dwell-time stability over a declared window, and a domain-specific validity predicate. Cost-accounting completeness is reported as a separate quality flag that determines whether cost-normalized claims are allowed. The framework defines rate- and cost-normalized observables for accepted transitions, separates fully measured claims from task-level and proxy estimates, and gives falsification criteria that prevent overclaiming from transient responses or post-hoc thresholds.

VSTF does not claim to replace transition path theory, first-passage analysis, stochastic thermodynamics, clinical prediction methodology, semi-Markov duration modeling, or domain-specific mechanistic models. Its contribution is narrower and operational: it changes which dynamically detected transitions are allowed to enter the numerator of a performance, validation, or intervention metric. The central question becomes: how many pre-specified, distinguishable, retained, domain-valid transitions does a system produce per unit time, energy, perturbation, or risk exposure, including failed attempts in the appropriate denominator? This formulation yields testable protocols in hardware benchmarking, ICU time-series evaluation, cancer tumor-state reset experiments, and motor-control rehabilitation studies.
\end{abstract}

\noindent\textbf{Keywords:} stabilized transitions; irreversible decisions; state validity; event-level metrics; information thermodynamics; metastability; physiological stability; cancer systems biology; neurorehabilitation; reproducibility.

\section{Motivation}

Scientific and engineering measurements often quantify \emph{activity}: power consumption, operations, events, spikes, biomarker expression, entropy production, signal amplitude, risk-score elevation, or perturbation response. Activity is necessary to understand a system, but it is not always the right unit for evaluating an outcome.

Four examples illustrate the problem.

\begin{enumerate}
  \item In hardware, high operation throughput does not guarantee that each operation corresponds to an accepted, retained, physically stable output. Energy may be spent on internal activity, failed attempts, rejected states, conversion overhead, readout, or correction.
  \item In clinical physiology, changing vital-sign dynamics may reflect destabilization, but a high dynamic score is not automatically a validated patient-level prediction, alarm, or clinical decision rule.
  \item In oncology, a perturbation may kill cells or alter expression acutely without durably moving surviving tissue away from a pathological tumor-state basin after withdrawal and rechallenge.
  \item In neurorehabilitation, electrical stimulation can evoke activity without proving voluntary control, retention, or a new motor regime.
\end{enumerate}

These cases share a hidden structure: the scientifically relevant event is not raw activity, but a transition that becomes stable, distinguishable, and valid under a declared operational rule. VSTF formalizes this common structure in the spirit of systems and complexity approaches that emphasize relations, state change, and context rather than isolated scalar activity \cite{sturmberg2014}.

\section{Scope and Non-Claims}

VSTF is deliberately narrow.

\begin{itemize}
  \item It is not a theory of everything.
  \item It does not introduce a new thermodynamic law.
  \item It does not claim that one scalar metric can replace domain-specific models.
  \item It does not assert that all biological, computational, or cosmological systems share one mechanism.
  \item It does not turn retrospective associations into causal validation.
  \item It does not claim novelty for transition rates, first-passage events, metastable basins, or reactive trajectories as such.
\end{itemize}

The framework provides a measurement grammar for a specific question:

\begin{quote}
\emph{When should a change in a physical, biological, or computational system be counted as a valid state transition rather than as transient activity?}
\end{quote}

The intended contribution is therefore:
\[
  \VSTF =
  \mathrm{dynamic\ transition}
  + \mathrm{operational\ acceptance}
  + \mathrm{retention}
  + \mathrm{cost\ boundary}
  + \mathrm{pre\mbox{-}specification}.
\]

The word ``framework'' is used intentionally. VSTF is a proposed operational framework and protocol grammar, not a consensus or standards-body standard. A stronger theory-level claim would require additional axioms, identifiability results, or nontrivial theorems beyond the operational framework developed here.

\section{Contributions and Defensible Novelty}

The defensible novelty of VSTF is not the existence of state transitions, transition rates, first-passage times, or metastable basins. Those objects are already central in dynamical systems theory, Transition Path Theory, Markov-state modeling, reliability engineering, and clinical prediction methodology. The contribution is the \emph{acceptance layer} placed between a dynamically detected transition and a scientific claim about outcome production. Table~\ref{tab:antecedents} summarizes the nearest antecedents and the incremental layer claimed here.

\begin{table}[h]
\centering
\caption{Nearest antecedents and the incremental VSTF layer.}
\label{tab:antecedents}
\begin{tabular}{p{0.24\linewidth}p{0.33\linewidth}p{0.33\linewidth}}
\toprule
Antecedent & What it already provides & What VSTF adds \\
\midrule
Transition Path Theory & Reactive trajectories, committors, fluxes, and transition rates between state sets. & Domain-valid outcome acceptance, retention as a reporting gate, rejected-event accounting, and claim-level restrictions. \\
First-passage and exit-time analysis & Hitting, exit, and dwell events under specified regions. & A protocol for deciding which detected events are scientifically countable as outcomes. \\
Reliability/yield metrics & Pass/fail counts and accepted outputs. & Cross-domain coupling of yield with latent-state localization, distinguishability, retention, and cost boundaries. \\
Clinical prediction standards & Calibration, discrimination, utility, external validation, and risk-of-bias assessment. & A state-transition vocabulary that prevents instability features from being called clinical events without validation. \\
Selective classification and reject-option rules & Risk--coverage tradeoffs, abstention under low confidence, and explicit rejected predictions \cite{chow1970reject,geifman2017selective}. & Temporal state-transition semantics, dwell/retention gates, domain validity, event ledgers, and cost-boundary accounting for accepted, rejected, and pending transition attempts. \\
Recurrent-event and counting-process methods & Event occurrence processes, rates, at-risk time, censoring, and repeated-event inference. & Definition of the admissible event universe to which those estimators are applied. \\
Endpoint, estimand, and adjudication frameworks & Domain-specific outcome definitions, endpoint hierarchy, surrogate-endpoint governance, estimand definitions, adjudication committees, and intercurrent-event handling. & Cross-domain generalization of event admissibility from clinical trials to dynamical trajectories and engineered systems. \\
Conformity assessment & Acceptance/rejection rules under measurement uncertainty, acceptance intervals, and producer/consumer risk. & Extension from static object conformity to temporally segmented dynamic transitions with retention, state semantics, rejected-event accounting, and cost-normalized reporting. \\
Temporal logic and runtime verification & Formal temporal predicates over traces or signals, including online monitoring and robustness of temporal specifications. & Measurement uncertainty, reference validity, domain admissibility, cost-boundary governance, rejected-event accounting, and claim restrictions around a temporal predicate. \\
Hidden semi-Markov and segmental models & Explicit state durations, latent segments, and dwell-time structure \cite{yu2010hsmm}. & A cross-domain adjudication layer that locks detection, retention, domain validity, and denominator governance before an event enters an outcome numerator. \\
Stochastic thermodynamics & Trajectory-level work, heat, entropy production, and fluctuation relations. & Separation of thermodynamic dissipation from accepted stabilized outcome counts. \\
\bottomrule
\end{tabular}
\end{table}

The manuscript is therefore intended as a methodological framework with three precise claims:

\begin{enumerate}
  \item Many domains confuse activity with outcome; VSTF formalizes a conservative adjudication layer that prevents this confusion.
  \item The accepted-transition numerator and the full-boundary denominator can change rankings, interpretations, and claims relative to raw activity or raw transition counts.
  \item The framework generates auditable decision rules and domain-specific falsifiable hypotheses because each domain claim must survive baseline comparison, locked thresholds, event segmentation, retention testing, and external or separate validation.
\end{enumerate}

More precisely, VSTF proposes a temporal extension and cross-domain synthesis of conformity-style decision rules for dynamically segmented transition events. Its claimed novelty lies in the locked composition of segmentation, state localization, measurement-system distinguishability, event-level retention, domain admissibility, and cost-boundary restrictions, not in any of these components separately. A static object may be accepted or rejected under measurement uncertainty; a VSTF event is accepted only after temporal segmentation, state localization, distinguishability, event-level retention, domain admissibility, and denominator accounting have all been declared. VSTF does not claim novelty for temporal segmentation, dwell-time modeling, hidden-state inference, or acceptance testing separately. Its proposed contribution is that these components are jointly locked before an event is admitted into the numerator of an outcome or cost-normalized metric.

The prior-art claim is deliberately scoped. Search terms used to position the framework included combinations of ``transition path theory'', ``first passage'', ``hidden semi-Markov'', ``runtime verification'', ``metric temporal logic'', ``conformity assessment'', ``measurement uncertainty decision rule'', ``recurrent event'', ``counting process'', ``endpoint adjudication'', ``clinical endpoint'', ``surrogate endpoint'', ``estimand'', ``intercurrent event'', and ``energy efficiency benchmark''. The search was used to identify nearby methodological families rather than to prove absence. In clinical applications, VSTF should be read together with surrogate-endpoint and estimand frameworks rather than as a substitute for them \cite{prentice1989,ich2019e9r1}. To our knowledge, VSTF is best described as a cross-domain synthesis and protocol grammar for event-level adjudication, not as a replacement for any one of these literatures.

\section{Notation}

Table~\ref{tab:notation} defines the notation used in the formal definitions, observables, and benchmark sections.

\begin{table}[h]
\centering
\caption{Core notation used throughout the framework.}
\label{tab:notation}
\begin{tabular}{p{0.22\linewidth}p{0.68\linewidth}}
\toprule
Symbol & Meaning \\
\midrule
$X_t$ & Latent system state at time $t$. \\
$Y_t$ & Observed data generated from $X_t$ through measurement model $P_M(Y_t\mid X_t)$. \\
$K_\theta$ & Latent transition kernel or dynamical model used when model-based filtering is claimed. \\
$\pi_t(C_j)$ & Filtering probability $\Pr_\theta(X_t\in C_j\mid Y_{0:t})$. \\
$\mathcal{X}$ & State space. \\
$C_i$ & Operational state region or basin. \\
$e_k$ & Candidate event segment. \\
$t_{\mathrm{start}}$ & Event start time under the locked segmentation rule. \\
$t_{\mathrm{commit}}$ & First time at which the target state commitment rule is satisfied. \\
$t_{\mathrm{end}}$ & Event end time after dwell, exit, return, or refractory conditions are resolved. \\
$\epsilon$ & Localization error tolerance. \\
$\alpha$ & Distinguishability error tolerance. \\
$\delta$ & Persistence or return-failure tolerance. \\
$\tau_\ast$ & Minimum declared persistence or retention window. \\
$V_{\mathrm{dom}}$ & Pre-specified domain-validity predicate. \\
$S_k$ & Three-state adjudication status in $\{\mathrm{accepted},\mathrm{rejected},\mathrm{pending}\}$. \\
$A_k$ & Mature hard accepted-transition indicator, $A_k=\1\{S_k=\mathrm{accepted}\}$. \\
$p_k^{\mathrm{ref}}$ & Posterior or estimated probability of candidate-level matched-reference validity, $\Pr(Z^{\mathrm{match}}_k=1\mid\mathcal{Y}_k,D_{\mathrm{train}})$. \\
$R_k(\tau_\ast)$ & Event-level retention indicator for candidate event $k$. \\
$\rho_j(\tau_\ast;\mathcal{P})$ & Population retention reliability for target region $C_j$ under protocol $\mathcal{P}$. \\
$Q_k^{\mathrm{acct}}$ & Accounting-quality flag for event $k$. \\
$Q_{\mathcal{B}}^{\mathrm{acct}}[a,b]$ & Window-level accounting-completeness flag for the full denominator in $[a,b]$. \\
$E_{\mathrm{sup}}^{\mathcal{B}}[a,b]$ & Full-boundary supplied energy or cost ledger over the declared window. \\
$N_{\VST}$ & Count of accepted valid stabilized transitions; unless otherwise stated, $N_{\VST}\equiv N_{\VST}^{\mathrm{op}}$. Reference-valid counts carry an explicit $\mathrm{ref}$ superscript or subscript. \\
$\widehat{\VSTR}$ & Finite-window accepted-transition rate. \\
$\widehat{\VSTE}$ & Finite-window energy-normalized accepted-transition yield. \\
$\VSTJ$ & Perturbation-normalized accepted-transition yield. \\
$\mathcal{B}$ & System boundary for energy, dose, time, readout, rejection, and stabilization costs. \\
\bottomrule
\end{tabular}
\end{table}

\section{State Space and Operational Partitions}

Let a system be represented by a latent state variable
\[
  X_t \in \mathcal{X},
\]
where $\mathcal{X}$ may be a microscopic phase space, a coarse-grained physical state space, a vector of clinical time-series features, a molecular module space, or an experimentally defined output space.

Measurements are represented separately:
\[
  Y_t \sim P_M(\,\cdot \mid X_t),
\]
where $P_M$ is the observation model induced by the measurement map $M$, sensor noise, preprocessing, classifier, and readout procedure. This distinction prevents the probability of a latent state from being conflated with statistical confidence about that state.

When the manuscript uses a latent posterior such as $\Pr(X_t\in C_j\mid Y_{0:t})$, the observation model is not sufficient by itself. A model-based posterior also requires a prior and latent dynamics, for example
\[
  X_0\sim p_0,\qquad
  X_{t+\Delta t}\sim K_\theta(\,\cdot\mid X_t),\qquad
  Y_t\sim P_M(\,\cdot\mid X_t).
\]
Only under such a model is the filtering probability
\[
  \pi_t(C_j)=\Pr_\theta(X_t\in C_j\mid Y_{0:t})
\]
well-defined. If a study instead uses a calibrated classifier score $\Pr(C_j\mid Y_t)$, it should be reported as classifier-based localization rather than as a latent-state posterior.

\begin{definition}[Operational state region]
An operational state region is a measurable subset
\[
  C_i \subset \mathcal{X}
\]
with a declared interpretation, measurement procedure, tolerance, and resolution rule. Regions may be fixed from prior knowledge or estimated from a training/reference cohort, but they must be frozen before confirmatory evaluation.
\end{definition}

The operational state family may be either a disjoint partition or an overlapping cover. If overlaps are permitted, the protocol must pre-specify tie-breaking, indeterminate-state handling, and transition-assignment rules. In particular, a trajectory segment in $C_i\cap C_j$ must not be retrospectively assigned to whichever state makes the confirmatory result stronger.

Examples include a latched digital output, a metastable attractor basin, a classified benchmark output, a high-\OSD{} tumor state, a low-\OSD{} reference state, or a motor-output state coupled to voluntary intent.

\begin{definition}[Candidate event segmentation]
A candidate event is a protocol-defined segment
\[
  e_k=(t_{\mathrm{start}},t_{\mathrm{commit}},t_{\mathrm{end}})
\]
constructed before outcome inspection. The segmentation rule must specify origin region, departure condition, target commitment condition, dwell window, hysteresis or refractory interval, and the rule by which a new event may begin.
\end{definition}

For a transition from $C_i$ to $C_j$, a typical segmentation rule is:
\[
  \mathrm{last\ exit\ from\ } C_i
  \rightarrow
  \mathrm{first\ commitment\ to\ } C_j
  \rightarrow
  \mathrm{minimum\ dwell\ or\ first\mbox{-}exit\ test}.
\]
This prevents boundary chatter or high sampling frequency from inflating event counts.

Segmentation rules must also declare whether they are online causal rules or offline retrospective rules. For an online detection claim, $t_{\mathrm{start}}$ and $t_{\mathrm{commit}}$ must be stopping times with respect to the observation filtration $\mathcal{F}^Y_t=\sigma(Y_s:s\leq t)$. A rule such as ``last exit from $C_i$ before later commitment to $C_j$'' may be valid for offline event adjudication, but it must not be used to claim real-time detection latency unless an equivalent causal stopping-time rule is specified.

All online VST claims must use filtered information available at the adjudication time. Smoothed posteriors or retrospective labels using future observations may be reported only as offline retrospective adjudication, not as real-time localization, retention, or latency evidence.

\begin{definition}[Candidate transition]
A candidate transition over an event segment $e_k$ is an observed or inferred movement
\[
  C_i \longrightarrow C_j,\qquad i \neq j,
\]
under a declared protocol $\pi$, measurement map $M$, and system boundary $\mathcal{B}$.
\end{definition}

The system boundary $\mathcal{B}$ specifies which energy, time, control, readout, stabilization, and attempt-level costs are charged to the transition. Accepted, rejected, and pending outcomes are reported as statuses of candidate attempts, while the ledger reports the functional costs incurred by those attempts.

\section{Assumptions Required for Confirmatory Use}

Exploratory uses of VSTF may be broader, but confirmatory claims require the following assumptions to be declared and assessed.

\begin{assumption}[Locked partition]
The state regions $C_i$, measurement map $M$, segmentation rule, tolerances $(\epsilon,\alpha,\delta,\tau_\ast)$, and domain predicate $V_{\mathrm{dom}}$ are fixed before the confirmatory outcome is inspected. If any element is tuned on the same outcome used for evaluation, the claim is exploratory unless independently revalidated.
\end{assumption}

\begin{assumption}[Specified and empirically qualified localization model]
Either an explicit latent process model $(p_0,K_\theta,P_M)$ is available for model-based filtering, or the measurement procedure yields a calibrated classifier score for classifier-based localization. Without one of these, localization should be reported as deterministic task-level acceptance or as an exploratory proxy, not as a posterior probability over latent states. Model specification alone does not guarantee identifiability: label switching, nonidentifiable parameters, or observationally equivalent latent configurations must be ruled out, constrained, or reported as a limitation before a confirmatory posterior-localization claim is made.
\end{assumption}

\begin{assumption}[Event separability]
Candidate event segments are separated by hysteresis, return-to-origin, refractory time, or another pre-specified rule sufficient to prevent repeated counting of a single unresolved transition.
\end{assumption}

\begin{assumption}[Event-granularity comparability]
Systems may be compared by $N_{\VST}$, \VSTR{}, or \VSTE{} only when their event-granularity rules are commensurate. Sampling rate, hysteresis width, refractory interval, dwell requirement, and commitment rule must be harmonized or stress-tested; otherwise one system can appear superior merely because its protocol splits one physical transition into more countable events.
\end{assumption}

\begin{assumption}[Boundary completeness for cost claims]
For \VSTE{}, \VSTJ{}, or cross-system cost comparison, the declared denominator includes all functional costs inside $\mathcal{B}$, including readout, stabilization, control, maintenance, conversion, and the corresponding costs incurred by accepted, rejected, and pending attempts. Excluded costs must be justified as outside the boundary rather than omitted for convenience.
\end{assumption}

\begin{assumption}[Baseline comparability]
Any claim of incremental value must compare VSTF against the nearest simpler baseline: raw activity, raw crossings, first-passage counts, TPT-derived rates, reliability yield, or standard clinical prediction metrics as appropriate.
\end{assumption}

\begin{assumption}[Admissible domain predicate]
The domain predicate must belong to an admissible predicate class $\mathcal{V}_{\mathrm{admissible}}$. A predicate is admissible only if it is pre-specified outside the confirmatory sample, measurable independently of threshold tuning, substantively linked to an external outcome or utility, nonredundant with localization, distinguishability, and event-level retention, and auditable by an external reader.
\end{assumption}

\section{Valid State Transitions}

\begin{definition}[Valid stabilized state transition]
A candidate transition $C_i \to C_j$ is a state-valid stabilized transition if it satisfies the first four conditions below. The fifth condition is an accounting-quality requirement for cost-normalized claims, not a prerequisite for the existence of the state transition.

\begin{enumerate}
  \item \textbf{Localization:} the post-transition state lies inside the declared target region with probability at least $1-\epsilon$. In model-based use, this is the filtering posterior
  \[
    \pi_{t_{\mathrm{commit}}}(C_j)
    =
    \Pr_\theta(X_{t_{\mathrm{commit}}}\in C_j\mid Y_{0:t_{\mathrm{commit}}})
    \geq 1-\epsilon.
  \]
  In classifier-based use, the corresponding calibrated score $\Pr(C_j\mid Y_{t_{\mathrm{commit}}})$ may be used, but it must not be described as a latent posterior without calibration and identifiability assumptions.
  To avoid circularity, the commitment rule and localization confirmation must be declared in one of two ways. Either the commitment threshold is explicitly the localization rule, in which case it is not counted as an independent confirmatory test, or discovery and validation are separated:
  \[
    t_{\mathrm{commit}}=\inf\{t:\pi_t(C_j)\geq\gamma_{\mathrm{gen}}\},
    \qquad
    \pi_{t_{\mathrm{commit}}+\ell}(C_j)\geq \gamma_{\mathrm{val}},
  \]
  with $\ell>0$ and $\gamma_{\mathrm{val}}$ fixed before testing.
  Because $t_{\mathrm{commit}}$ is then a data-adaptive stopping time, calibration should also be checked under the event-triggered rule itself, for example by estimating
  \[
    \Pr(X_{t_{\mathrm{commit}}}\in C_j
    \mid \pi_{t_{\mathrm{commit}}}\in B,\ t_{\mathrm{commit}}<\infty)
  \]
  over probability bins $B$, or by using hold-out observations that were not used to trigger commitment.
  \item \textbf{Distinguishability:} $C_j$ is operationally distinguishable from competing regions under a declared decision rule
  \[
    d_M:Y\rightarrow\{1,\ldots,K\}.
  \]
  A one-sided class-conditional sensitivity requirement is not sufficient by itself, because a degenerate rule $d_M(Y)\equiv j$ can achieve zero false negatives for class $j$ while failing to distinguish $C_j$ from every competing region. A valid distinguishability protocol must therefore control both target sensitivity and cross-class false assignment:
  \[
    \Pr[d_M(Y)=j\mid X\in C_j]\geq 1-\alpha_j,
    \qquad
    \Pr[d_M(Y)=j\mid X\in C_i]\leq\beta_{ij},
    \quad i\neq j.
  \]
  In multiclass settings, report a locked confusion criterion, for example
  \[
    \max_i \Pr[d_M(Y)\neq i\mid X\in C_i]\leq\alpha
  \]
  together with the full confusion matrix
  \[
    E_{jk}=\Pr[d_M(Y)=k\mid X\in C_j].
  \]
  Distinguishability can be treated as a protocol-level validation flag $Q_{\mathrm{dist}}(M,\{C_i\})=1$ for the measurement system, or as a transition-specific validation flag $Q_{\mathrm{dist},ij}=1$ for the particular candidate transition $C_i\to C_j$; localization remains event-specific.
  Pairwise Bayes error may be used only after specifying priors and loss function, e.g. $e_{\mathrm{Bayes}}(P_j,P_k;\pi_j,\pi_k,L)$.
  Confirmatory distinguishability should be qualified with finite-sample uncertainty. For example, a point estimate $\hat e_i\leq\alpha_i$ is insufficient by itself; the locked rule should require an upper confidence or credible bound $U_{1-\eta}(e_i)\leq\alpha_i$, or an equivalent pre-specified decision-risk criterion.
  \item \textbf{Persistence:} the state remains in the target region over a declared operational window. Define the first-exit time
  \[
    T^{(j)}_{\mathrm{exit}}=\inf\{t>t_{\mathrm{commit}}:X_t\notin C_j^{\mathrm{out}}\}.
  \]
  Here a protocol may use nested hysteresis regions $C_j^{\mathrm{in}}\subset C_j^{\mathrm{out}}$: commitment is triggered by entry into the inner region, while retention failure is triggered only by exit from the outer region. In discrete-time data, first exit is observed only on the sampling grid unless interval censoring or continuous-time reconstruction is explicitly modeled.
  Define the event-level retention indicator
  \[
    R_k(\tau_\ast)
    =
    \1\{T^{(j)}_{\mathrm{exit}}-t_{\mathrm{commit}}\geq \tau_\ast\}.
  \]
  A specific event is retained when $R_k(\tau_\ast)=1$. Separately, the population retention reliability for target region $C_j$ is
  \[
    \rho_j(\tau_\ast;\mathcal{P})=
    \Pr(T^{(j)}_{\mathrm{exit}}-t_{\mathrm{commit}}\geq \tau_\ast
    \mid t_{\mathrm{commit}}<\infty,\ e_k\ \mathrm{generated\ by\ protocol}\ \mathcal{P}).
  \]
  This definition conditions on the selection induced by the commitment protocol rather than on an arbitrary state-time sample. A protocol may require $\rho_j(\tau_\ast;\mathcal{P})\geq1-\delta$ as a population reliability property, preferably with finite-sample uncertainty control, but $A_k$ must use the event-level retention evidence available for event $k$.
  If the domain concerns resilience rather than persistence, define a separate return time
  \[
    T^{(j)}_{\mathrm{return}}=\inf\{t>T^{(j)}_{\mathrm{exit}}:X_t\in C_j\}
  \]
  and report it as a distinct property.
  If $X_t$ is latent, persistence must also be evaluated through a filtering or smoothing rule, a calibrated observed-state surrogate, or a censoring rule for unresolved retention windows.
  \item \textbf{Domain validity:} the event passes a domain-specific predicate $V_{\mathrm{dom}}=1$ defined before outcome inspection. The predicate must be admissible in the sense above: measurable, fixed before confirmatory testing, independent of threshold tuning on the test endpoint, nonredundant with the other VST predicates, and externally or separately validated when it is used for a substantive claim.
  \item \textbf{Accounting quality:} the supplied work, dissipated heat, entropy production, dose, rejected attempts, readout, control, or stabilization ledger associated with the protocol is reported over the declared boundary $\mathcal{B}$.
\end{enumerate}
\end{definition}

The domain predicate prevents generic state movement from being misinterpreted as success. In hardware, the predicate may be retention and accepted output. In oncology, it may be movement toward a frozen low-\OSD{} reference after washout and rechallenge. In rehabilitation, it may be dependence of movement on voluntary intent rather than stimulation alone. To reduce researcher degrees of freedom, a confirmatory domain predicate should be a frozen audit object of the form
\[
  V_{\mathrm{dom},k}=\1\{h(W_k;\psi)\in\mathcal{A}_{\mathrm{dom}}\},
\]
where $W_k$ is the data packet reserved for domain adjudication, $h$ is a declared mapping, $\psi$ is frozen outside the confirmatory set, and $\mathcal{A}_{\mathrm{dom}}$ is a pre-registered acceptance region. If $W_k$ reuses observations that also drive localization or retention, that dependence must be declared and propagated rather than treated as independent evidence.

\begin{definition}[Detected and reference event sets]
For a locked protocol $\mathcal{P}$, let
\[
  \widehat{\mathcal{E}}_{\mathcal{P}}(Y)=\{\hat e_1,\ldots,\hat e_{N_{\mathrm{cand}}}\}
\]
denote the detected candidate-event set generated from the observed trajectory. When a latent simulator or independently justified reference standard is available, let
\[
  \mathcal{E}^{\ast}_{\mathrm{ref}}=\{e^{\ast}_1,\ldots,e^{\ast}_{N_{\mathrm{ref}}}\}
\]
denote the reference-event set. Operational and reference events are not assumed to share an index. Their correspondence must be established by a pre-specified one-to-one matching rule
\[
  m:\widehat{\mathcal{E}}_{\mathcal{P}}\leftrightarrow\mathcal{E}^{\ast}_{\mathrm{ref}},
\]
based on admissible temporal overlap, state overlap, commitment-time tolerance, or a declared composite cost. For example, one may minimize a locked cost
\[
  c_{kr}=w_1|t^{(k)}_{\mathrm{commit}}-t^{(r)\ast}_{\mathrm{commit}}|
  +w_2\{1-\mathrm{IoU}(I_k,I_r^\ast)\},
\]
subject to a maximum admissible cost, a deterministic tie-breaking rule, and one-to-one assignment. The tuple $(c_{\mathrm{match}},c_{\max},\mathrm{tie\ rule})$ is part of the locked protocol $\mathcal{P}$. Unmatched accepted candidate events contribute to false acceptance; unmatched reference-valid events contribute to false rejection.
\end{definition}

\begin{definition}[Operational decision and matched reference validity]
For detected candidate event $\hat e_k$, define the mature localization/segmentation gate
\[
  Q^{\mathrm{loc}}_k
  =
  \1\{t_{\mathrm{commit},k}<\infty,\ \pi_{t_{\mathrm{commit},k}+\ell}(C_j)\geq\gamma_{\mathrm{val}}\},
\]
with the corresponding calibrated-score version used in classifier-based protocols. Let each mature or immature candidate event receive a three-state adjudication status
\[
S_k\in\{\mathrm{accepted},\mathrm{rejected},\mathrm{pending}\}.
\]
The status is assigned by the locked rule
\[
S_k =
\begin{cases}
\mathrm{accepted}, & \text{all required mature gates pass},\\
\mathrm{rejected}, & \text{at least one mature required gate definitively fails},\\
\mathrm{pending}, & \text{no mature required gate has failed, but at least one required gate is not yet mature}.
\end{cases}
\]
Equivalently, for mature events the operational VSTF acceptance indicator is
\[
  A_k=\1\{S_k=\mathrm{accepted}\} =
  Q_{\mathrm{dist},ij}(\mathcal{P},M,\{C_i\})
  Q^{\mathrm{loc}}_k
  R_k(\tau_\ast)
  V_{\mathrm{dom},k}.
\]
Here $Q_{\mathrm{dist},ij}(\mathcal{P},M,\{C_i\})\in\{0,1\}$ is a protocol-level or transition-specific measurement-system qualification flag for the intended transition $C_i\to C_j$, $Q^{\mathrm{loc}}_k$ is event-level localization and segmentation success, $R_k(\tau_\ast)$ is event-level retention, and $V_{\mathrm{dom},k}$ is event-level domain admissibility. This separates a reusable confusion-matrix or distinguishability validation from predicates that are evaluated on each event. Pending events are neither accepted nor counted as failures until their required gates mature. Only detected candidate events with $S_k=\mathrm{accepted}$ enter the operational VST count
\[
  N_{\VST}^{\mathrm{op}}=\sum_k A_k=\sum_k\1\{S_k=\mathrm{accepted}\}.
\]

When a detected candidate event is matched to a latent or independently adjudicated reference event, define separately
\[
  Z^{\mathrm{match}}_k\in\{0,1\},
\]
where $Z^{\mathrm{match}}_k=1$ means that the matched reference event is valid under the target state, retention, and domain-validity semantics according to that reference standard, independently of whether the measurement rule accepted the detected candidate. In hardware benchmarks this may be a latent simulator or reference state; in clinical or biological settings it is often a fallible but independently justified reference standard. In noiseless task benchmarks $A_k$ and matched reference validity may coincide, but under measurement uncertainty they are distinct. Let $m\subset\mathcal{K}\times\mathcal{R}$ be the locked one-to-one matching relation between detected candidates and reference events, and let $m_A=\{(k,r)\in m:A_k=1\}$. Error rates must be defined after matching:
\begin{align}
  TP &= \#\{(k,r)\in m_A:Z_r^\ast=1\},\\
  FP &= \#\{k:A_k=1,\ k\notin\mathrm{dom}(m)\}
        +\#\{(k,r)\in m_A:Z_r^\ast=0\},\\
  FN &= \#\{r:Z_r^\ast=1,\ r\notin\mathrm{rng}(m_A)\}.
\end{align}
Without an explicit reference-event universe and matching rule, reported FP and FN are candidate-universe error rates only; they must not be interpreted as sensitivity to all latent transitions.
\end{definition}

Accounting completeness is represented separately:
\[
  Q^{\mathrm{acct}}_k\in\{0,1\}.
\]
An event may be a valid state transition while being unusable for event-level cost attribution if $Q^{\mathrm{acct}}_k=0$. Window-level energy-normalized comparison additionally requires a complete denominator flag
\[
  Q_{\mathcal{B}}^{\mathrm{acct}}[a,b]\in\{0,1\}.
\]
Incomplete accounting invalidates \VSTE{} or cost-yield claims, not necessarily the existence of the state transition.

\begin{definition}[Probabilistic matched-reference validity]
When state-validity predicates cannot be observed without error, the hard operational decision $A_k$ should be accompanied by a posterior probability of latent or reference-standard validity,
\[
  p_k^{\mathrm{ref}}=\Pr(Z^{\mathrm{match}}_k=1\mid \mathcal{Y}_k,D_{\mathrm{train}}),
  \qquad
  \E[N_{\mathrm{ref,cand}}\mid Y,D_{\mathrm{train}}]=\sum_{\hat e_k\in\widehat{\mathcal{E}}_{\mathcal{P}}} p_k^{\mathrm{ref}}.
\]
Here $\mathcal{Y}_k$ is the event-specific data packet: the observations used for commitment, validation, retention, distinguishability, domain validity, matching, and censoring status. This quantity estimates reference validity among detected candidates. If the reference standard itself is imperfect, $p_k^{\mathrm{ref}}$ is validity relative to the declared standard, not automatic truth. A confirmatory protocol should either model reference-standard sensitivity and specificity, include adjudicator disagreement or repeated-measurement uncertainty, or explicitly label the resulting quantity as reference-standard-relative VST yield. It is not, without an explicit event-detection model, an estimator of the total number of reference-valid latent transitions
\[
  N_{\mathrm{ref,total}}=\#\{e_r^\ast\in\mathcal{E}^{\ast}_{\mathrm{ref}}:Z_r^\ast=1\}.
\]
If $A_k$ is a deterministic function of $\mathcal{Y}_k$ under a locked protocol, then $\Pr(A_k=1\mid\mathcal{Y}_k)=A_k$ is trivial; the nontrivial uncertainty concerns matched reference validity, not the existence of the operational decision. The variance of the probabilistic matched-reference-valid count should be reported as
\[
  \mathrm{Var}\!\left(\sum_k Z^{\mathrm{match}}_k\mid \mathcal{Y},D_{\mathrm{train}}\right)
  =
  \sum_k p_k^{\mathrm{ref}}(1-p_k^{\mathrm{ref}})
  +2\sum_{k<\ell}\mathrm{Cov}(Z^{\mathrm{match}}_k,Z^{\mathrm{match}}_\ell\mid \mathcal{Y},D_{\mathrm{train}}),
\]
with the covariance term retained or estimated when events are not independent. This distinguishes certain accepted events from borderline threshold crossings.

\begin{quote}
\textbf{Separation principle.} The latent or reference-standard validity of an event, the measurement record, the operational decision, and the scientific claim are distinct objects:
\[
  e_r^\ast,\ Z_r^\ast\ \neq\ \mathcal{Y}_k\ \neq\ A_k\ \neq\ \mathrm{claim}.
\]
The measurement record $\mathcal{Y}_k$ supports an operational adjudication $A_k$ under a locked protocol, and may support posterior statements about matched reference validity under an explicit validation and matching model. A scientific claim is then restricted by the declared evidence level, baseline comparison, uncertainty analysis, domain predicate, and denominator completeness.
\end{quote}

The validity predicates are a logical conjunction, not a collection of statistically independent tests. Localization, distinguishability, retention, and domain validity may be correlated, especially when multiple predicates are derived from the same classifier, sensor stream, or posterior. Uncertainty propagation must therefore retain predicate dependence rather than multiplying marginal pass probabilities unless independence has been justified.

When $p_k^{\mathrm{ref}}=\Pr(Z^{\mathrm{match}}_k=1\mid\mathcal{Y}_k,D_{\mathrm{train}})$ depends on estimated model parameters $\theta$--for example $p_0$, $K_\theta$, $P_M$, classifier calibration, matching tolerance, or a covariance model--event uncertainty should be separated from parameter uncertainty:
\[
  \mathrm{Var}(N_{\mathrm{ref,cand}}\mid D)
  =
  \E_\theta\!\left[\mathrm{Var}(N_{\mathrm{ref,cand}}\mid D,\theta)\mid D\right]
  +
  \mathrm{Var}_\theta\!\left(\E[N_{\mathrm{ref,cand}}\mid D,\theta]\mid D\right).
\]
The first term is event uncertainty; the second is model or parameter uncertainty. Posterior sampling, nested bootstrap, clustered bootstrap, or another pre-specified procedure should be used when model-based probabilities are reported. The probability model used for $p_k^{\mathrm{ref}}$ should be calibrated out of sample or validated outside the confirmatory set; otherwise $\sum_k p_k^{\mathrm{ref}}$ is only a model-implied count under the fitted assumptions.
\end{definition}

\section{Core Observables}

Let $N_{\VST}^{\mathrm{op}}(\Delta t)$ be the number of operationally accepted valid transitions over acquisition interval $\Delta t$:
\[
  N_{\VST}^{\mathrm{op}}(\Delta t)=\sum_{k\in\Delta t} A_k.
\]
For finite windows, an event must be assigned a pre-specified index time, for example
\[
  t_k^{\mathrm{index}}=t_{k,\mathrm{commit}}
  \quad\mathrm{or}\quad
  t_k^{\mathrm{index}}=t_{k,\mathrm{end}}.
\]
Events whose retention window extends beyond the end of observation are pending or right-censored; they should not be automatically counted as rejected. Retention-based estimands must therefore distinguish the acquisition window from the follow-up horizon.

\subsection{Multistate and Competing Transitions}

In multistate systems, transition adjudication may require competing-risk or multistate-process treatment. A commitment to one target region can preclude, censor, or alter the risk set for another target event, and target-specific \VSTR{} estimates should not be interpreted as independent binary processes unless that independence is justified by the domain model. A protocol comparing $C_i\to C_j$ with $C_i\to C_\ell$ must declare the priority rule, censoring rule, and shared risk-set convention before outcome inspection.

\begin{definition}[Finite-window valid state transition rate]
For a declared observation window $\Delta t$, the finite-window valid state transition rate is
\[
  \widehat{\VSTR}_{\Delta t}=\frac{N_{\VST}(\Delta t)}{\Delta t}.
\]
If stationarity, ergodicity, and event-definition stability are justified, one may additionally define the asymptotic rate
\[
  \VSTR = \lim_{\Delta t\to\infty}\frac{N_{\VST}(\Delta t)}{\Delta t}.
\]
This requires more than stationarity of the raw latent dynamics: the induced accepted-event point process must be stationary or ergodic with finite intensity, or the corresponding renewal process must have finite mean inter-event time. In nonstationary biological or clinical systems, finite-window accepted-event frequency is the primary VSTF quantity. An asymptotic rate should be reported only when the accepted-event process admits a stable long-run intensity under the locked event definition.
\end{definition}

\subsection{Sampling-Resolution Robustness}

Hysteresis, refractory intervals, and dwell rules reduce boundary chatter, but they do not by themselves prove sampling invariance. A confirmatory VSTF analysis should include a sampling-resolution stress test whenever the observation grid can alter event identity. A minimal grid is
\[
  \Delta t\in\{\Delta t_0/4,\Delta t_0/2,\Delta t_0,2\Delta t_0,4\Delta t_0\}.
\]
For each grid, the analysis should report $N_{\mathrm{crossing}}$, $N_{\VST}^{\mathrm{op}}$, \VSTR{}, \VSTE{}, PPV, sensitivity, and the relative change in accepted-event count compared with $\Delta t_0$. The confirmatory protocol should declare the maximum admissible change before outcome inspection, or else label grid-sensitive findings as resolution-dependent.

\begin{definition}[Candidate yield and at-risk intensity]
When event opportunities differ across systems because of refractory intervals, dwell requirements, or sampling cadence, \VSTR{} should be accompanied by
\[
  Y_{\VST}=\frac{N_{\VST}}{N_{\mathrm{candidate}}},
  \qquad
  \lambda_{\mathrm{at\mbox{-}risk}}=
  \frac{N_{\VST}}{T_{\mathrm{at\mbox{-}risk}}},
\]
where $N_{\mathrm{candidate}}$ is the number of eligible candidate attempts and $T_{\mathrm{at\mbox{-}risk}}$ excludes intervals in which a new event could not arise by protocol. These quantities do not replace \VSTR{}; they prevent a refractory or segmentation rule from masquerading as a biological, computational, or energetic advantage.
\end{definition}

\begin{definition}[Energy-normalized valid transition yield]
Let $E_{\mathrm{sup}}^{\mathcal{B}}[a,b]$ be the supplied energy within the declared boundary, including accepted events, rejected attempts, readout, stabilization, conversion, control, maintenance, idle cost, and failed attempts when they are inside the boundary. Energy-normalized comparison is valid only when $Q_{\mathcal{B}}^{\mathrm{acct}}[a,b]=1$. Then
\[
  \widehat{\VSTE}_{[a,b]}=
  \frac{N_{\VST}[a,b]}{E_{\mathrm{sup}}^{\mathcal{B}}[a,b]}.
\]
Equivalently, if $N_{\VST}>0$,
\[
  \widehat{\VSTE}_{[a,b]} = \frac{1}{\bar{E}_{\mathrm{amortized,VST}}},
\]
where
\[
  \bar{E}_{\mathrm{amortized,VST}}
  =
  \frac{E_{\mathrm{sup}}^{\mathcal{B}}[a,b]}{N_{\VST}[a,b]}.
\]
This is the full-boundary amortized energy per accepted transition, not the mean energy of accepted events alone.
\end{definition}

\begin{definition}[Perturbation-normalized valid transition yield]
For biological or control experiments where energy is not the only meaningful denominator, let $P_{\mathrm{dose}}$ denote a declared perturbation burden, dose, exposure, risk, or intervention cost. Then
\[
  \VSTJ = \frac{N_{\VST}}{P_{\mathrm{dose}}}
\]
measures valid transitions per perturbation burden.
\end{definition}

\section{Estimation, Uncertainty, and Robustness}

\subsection{Finite-Window Estimands}

The primary estimands are finite-window quantities:
\[
  \widehat{\VSTR}_{[a,b]}=
  \frac{\sum_k A_k\,\1\{t_k^{\mathrm{index}}\in[a,b]\}}{b-a},
  \qquad
  \widehat{\VSTE}_{[a,b]}=
  \frac{\sum_k A_k\,\1\{t_k^{\mathrm{index}}\in[a,b]\}}{E_{\mathrm{sup}}^{\mathcal{B}}[a,b]}
  \quad \mathrm{if}\quad Q_{\mathcal{B}}^{\mathrm{acct}}[a,b]=1.
\]
Finite-window estimands are preferred because many biological, clinical, and adaptive computational systems are nonstationary. Asymptotic rates should be reported only with explicit stationarity or ergodicity justification.

If retention is required for $\tau_\ast$ after commitment, the acquisition window $[a,b]$ must be distinguished from the maturity horizon. The mature-window estimand is
\[
  \widehat{\VSTR}^{\mathrm{mature}}_{[a,b]}=
  \frac{\sum_k A_k\,\1\{t_{k,\mathrm{commit}}\in[a,b]\}}{b-a},
\]
but it is valid only when follow-up is available at least through $b+\tau_\ast$ or through the declared event-specific retention horizon. If follow-up ends at $b$, a complete-case estimand should instead be written as
\[
  \widehat{\VSTR}^{\mathrm{complete}}_{[a,b-\tau_\ast]}=
  \frac{\sum_k A_k\,\1\{t_{k,\mathrm{commit}}\in[a,b-\tau_\ast]\}}{b-a-\tau_\ast},
\]
or replaced by a censoring-aware estimator. Events committed near the right edge of the acquisition window are pending, not failures, unless their retention window is actually observed and fails.

\subsection{VSTE Window Alignment}

For \VSTE{}, the numerator and denominator must use a declared alignment convention. Under a \emph{calendar-window ledger},
\[
  \widehat{\VSTE}^{\mathrm{calendar}}_{[a,b]}=
  \frac{N_{\VST}[a,b]}{E_{\mathrm{sup}}^{\mathcal{B}}[a,b]},
\]
all energy physically supplied inside $[a,b]$ is counted and events crossing the boundary follow locked edge-censoring rules. Under an \emph{event-complete ledger},
\[
  \widehat{\VSTE}^{\mathrm{event}}_{[a,b]}=
  \frac{\sum_{k:t_k^{\mathrm{index}}\in[a,b]} A_k}
  {\sum_{k:t_k^{\mathrm{index}}\in[a,b]} E_k + E_{\mathrm{shared,allocated}}},
\]
event-specific costs are assigned to complete indexed events and shared costs are allocated by a pre-specified rule. A study must not mix an event-indexed numerator with an unrelated calendar denominator without stating the boundary convention.

\subsection{Uncertainty for Counts and Rates}

Poisson count models may be used when a Poisson or conditionally Poisson event-process assumption is substantively and diagnostically justified. Negative-binomial models may be used for a specified form of overdispersion. Temporal, subject-level, batch-level, recurrent-event, or refractory-period dependence should instead be addressed through an appropriate counting-process, frailty, clustered, renewal, mixed-effects, or resampling model.

For probabilistic reference-validity reporting, report
\[
  \widehat{N}_{\mathrm{ref,cand}}^{\,p}=\sum_k p_k^{\mathrm{ref}}
\]
alongside the hard operational count $N_{\VST}^{\mathrm{op}}=\sum_k A_k$. A large discrepancy between $\sum_k A_k$ and $\sum_k p_k^{\mathrm{ref}}$ indicates operational--probabilistic discordance that requires diagnosis through calibration, threshold-sensitivity, reference-standard, clustering, and model-uncertainty analyses. It should downgrade the claim unless the discrepancy is explained by the locked estimand.

\subsection{Threshold Sensitivity}

Every confirmatory VSTF analysis should include a sensitivity grid over the main thresholds:
\[
  \epsilon,\quad \alpha,\quad \delta,\quad \tau_\ast,\quad
  h_{\mathrm{hyst}},\quad r_{\mathrm{dead}},\quad \theta_{\mathrm{dom}},
\]
where $h_{\mathrm{hyst}}$ is hysteresis width, $r_{\mathrm{dead}}$ is refractory/dead time, and $\theta_{\mathrm{dom}}$ denotes domain-specific acceptance thresholds. The analysis should report whether conclusions are stable in an admissible neighborhood around the locked operating point.

\begin{criterion}[Threshold fragility]
If the main conclusion reverses under small, scientifically reasonable changes in event segmentation, localization tolerance, distinguishability tolerance, persistence window, or domain threshold, the result is not confirmatory.
\end{criterion}

\subsection{Missingness and Drift}

Missing data, sensor drift, batch effects, and classifier recalibration can create false transitions. A confirmatory analysis must predefine imputation, censoring, recalibration, and drift-handling rules. Events whose validity depends on post-hoc repair of missing data should be flagged separately and excluded from primary full-claim counts unless the repair procedure was locked in advance.

\section{Related Work and Incremental Contribution}

\subsection{Transition Path Theory and First-Passage Methods}

Transition Path Theory (TPT) provides a rigorous statistical theory of reactive trajectories between pre-specified sets $A$ and $B$, including committor functions, reactive currents, fluxes, hitting distributions, and transition rates \cite{e2006,metzner2009}. First-passage and first-exit methods similarly define events by hitting or leaving specified regions. These literatures are the closest formal antecedents to the dynamic-transition component of VSTF.

VSTF should therefore not be read as claiming that transition rates or reactive trajectories are new. The incremental claim is different:

\begin{quote}
TPT asks how a system dynamically moves between state sets. VSTF asks which dynamically detected transitions may be counted as valid outcomes under a pre-specified acceptance, retention, and cost protocol.
\end{quote}

Thus, a VST event can be implemented on top of a TPT, Markov-state, renewal-process, or first-passage event detector. The VSTF layer adds domain validity, retained outcome status, accepted/rejected accounting, and claim-level restrictions.

\subsection{Reliability, Yield, and Benchmarking}

Reliability engineering, benchmark validation, clinical prediction methodology, and assay-yield reporting already contain parts of the VSTF logic: pass/fail criteria, calibration, external validation, accepted tasks, error rates, and denominators. VSTF does not replace these standards. It generalizes a common rule across domains: never put raw activity in the numerator when the scientific claim concerns accepted stabilized outcomes.

\subsection{Conformity Assessment Under Measurement Uncertainty}

Metrological conformity assessment is a close antecedent. NIST and JCGM/ISO guidance formalize decision rules for accepting or rejecting objects under measurement uncertainty, including acceptance intervals and producer/consumer risk \cite{phillips2014,jcgm106}. VSTF should not claim novelty for the generic idea that uncertain measurements require acceptance rules. Its narrower contribution is to extend this logic to temporally segmented dynamic transitions, where the object under assessment is not a static item but an event with origin, commitment, retention, domain validity, rejected attempts, and a cost boundary.

\subsection{Temporal Logic and Runtime Verification}

Metric Temporal Logic, Signal Temporal Logic, and runtime verification are close formal antecedents for the temporal-predicate component of VSTF. Signal Temporal Logic was introduced to monitor temporal properties of continuous signals, and robust temporal-logic semantics quantify not only Boolean satisfaction but also distance from violation \cite{maler2004,fainekos2009}. Runtime-verification methods similarly build monitors for trace properties, including online or partial-trace monitoring in embedded and real-time systems \cite{reinbacher2014rv}.

VSTF should therefore not be read as claiming novelty for formulas of the form ``enter a target region and remain there for $\tau_\ast$''. A VSTF event can often be decomposed as
\[
  \phi_{\mathrm{temporal}}
  \wedge Q_{\mathrm{measurement}}
  \wedge V_{\mathrm{dom}}
  \wedge Q_{\mathcal{B}}^{\mathrm{acct}}
  \wedge C_{\mathrm{claim}},
\]
where $\phi_{\mathrm{temporal}}$ is a trajectory requirement that could be expressed in an STL/MTL-like language. The incremental VSTF layer is the governance around that temporal predicate: observation uncertainty, reference-standard validity, domain admissibility, denominator completeness, rejected-event accounting, and restrictions on what scientific claim may be made. If an existing temporal-logic or runtime-verification monitor plus domain standards already supplies the same acceptance and denominator governance, VSTF should be described as a translation rather than as an independent new mechanism.

\section{Relation to Established Theory}

\subsection{Conditional Thermodynamic Specialization and Resource Bounds}

Landauer's principle establishes a lower bound for logically irreversible erasure \cite{landauer1961,bennett2003,berut2012}. VSTF does not replace this bound, and it does not assign a Landauer cost to arbitrary attractor stabilization, biological state change, clinical prediction, or motor recovery. Landauer-type reasoning is relevant only when a VST event contains a well-defined logically irreversible information operation, such as a many-to-one logical map or erasure.

For physical implementations where a thermodynamic subsystem is explicitly specified, the conservative statement is an implementation-specific ensemble relation
\[
  \langle W_{\mathrm{th},k}\rangle \geq \Delta F_k
\]
under the assumptions appropriate to that subsystem. This is not, by itself, a deterministic lower bound on every individually accepted stochastic realization. Under an explicitly specified isothermal stochastic-thermodynamic convention, with a fixed sign convention, bath model, and unit convention, dissipated work may be related to entropy production, for example as
\[
  \langle W_{\mathrm{th},k}\rangle-\Delta F_k
  =
  T\langle\Sigma_k\rangle
  \geq 0
\]
when $\Sigma_k$ has units of entropy and the convention defines positive dissipation in that direction. If $\Sigma_k$ is dimensionless, the corresponding term is $k_B T\langle\Sigma_k\rangle$. The manuscript therefore treats $\Sigma_k$ only after specifying units, sign conventions, bath model, dynamics, and what portion of the implementation is inside the thermodynamic subsystem.

Engineering and protocol costs are accounted for separately from the thermodynamic subsystem. Over a declared window, functional cost categories should be separated from outcome-status labels:
\[
  E_{\mathrm{sup}}^{\mathcal{B}}[a,b]
  =
  E_{\mathrm{shared/idle}}^{\mathcal{B}}
  +
  \sum_{a\in\mathcal{C}_{[a,b]}}
  \left(
  E_{\mathrm{th}}^{\mathcal{B}}
  +E_{\mathrm{control}}^{\mathcal{B}}
  +E_{\mathrm{readout}}^{\mathcal{B}}
  +E_{\mathrm{conversion}}^{\mathcal{B}}
  +E_{\mathrm{maintenance}}^{\mathcal{B}}
  \right)_a,
\]
where each candidate attempt $a$ has a separate outcome label $S_a\in\{\mathrm{accepted},\mathrm{rejected},\mathrm{pending}\}$. Rejected, accepted, and pending are statuses of attempts, not mutually exclusive functional energy categories. A rejected attempt may still contain control, readout, conversion, and thermodynamic costs, so the ledger must avoid counting the same physical energy both as a functional category and as an outcome category. This separation is necessary because control, readout, and feedback processes may themselves dissipate energy; counting them once as entropy production and again as overhead would double count.

Let $\mathcal{A}=\{k:A_k=1\}$ be the accepted event set in $[a,b]$. A sample-level bound on \VSTE{} follows only when the implementation provides an independently justified eventwise resource minimum
\[
  E_{\mathrm{req},k}^{\mathcal{B}}\geq E_{\min,k}^{\mathcal{B}}>0
\]
for every accepted event. The sum $\sum_{k\in\mathcal{A}}E_{\min,k}^{\mathcal{B}}$ is valid only when the lower-bound contributions are samplewise, additive, and allocated to nonoverlapping event-specific resource segments. Shared setup, idle, controller, or readout costs must be assigned to the window ledger rather than counted repeatedly inside every event. If all additional ledger terms are nonnegative, then
\[
  E_{\mathrm{sup}}^{\mathcal{B}}[a,b]\geq \sum_{k\in\mathcal{A}}E_{\min,k}^{\mathcal{B}}.
\]
Thus
\[
  \widehat{\VSTE}_{[a,b]}
  =
  \frac{N_{\VST}[a,b]}{E_{\mathrm{sup}}^{\mathcal{B}}[a,b]}
  \leq
  \frac{N_{\VST}[a,b]}{\sum_{k\in\mathcal{A}}E_{\min,k}^{\mathcal{B}}}
  =
  \frac{1}{\bar{E}_{\min,\mathcal{A}}^{\mathcal{B}}},
\]
where $\bar{E}_{\min,\mathcal{A}}^{\mathcal{B}}=N_{\VST}^{-1}\sum_{k\in\mathcal{A}}E_{\min,k}^{\mathcal{B}}$. Only in a specific physical implementation where a separate derivation justifies substituting $E_{\min,k}^{\mathcal{B}}=\Delta F_k>0$ as an eventwise resource bound does this become a free-energy bound. If a uniform samplewise bound $E_{\min,k}^{\mathcal{B}}\geq E_{\min}^{\mathcal{B}}>0$ is justified for all accepted events, then $\widehat{\VSTE}_{[a,b]}\leq 1/E_{\min}^{\mathcal{B}}$. No useful positive bound of this type follows from events with nonpositive eventwise lower bounds or from ensemble-average thermodynamic relations alone.

Because VSTF often involves measurement, readout, feedback, and post-selection on accepted events, any use of work/free-energy equalities must declare whether the controller is inside the boundary and whether conditioning on acceptance changes the relevant work distribution. Nonequilibrium work relations such as Jarzynski's equality and feedback-control generalizations constrain ensemble statistics of work and show that measurement and information terms can alter the relevant work relations \cite{jarzynski1997,sagawa2010}. They should not be interpreted as deterministic lower bounds on every accepted stochastic trajectory without an additional derivation. These thermodynamic relations are external physical specializations that may be added to a VSTF protocol; they are not supplied by VSTF itself and are not the general foundation of the framework.

\begin{quote}
\textbf{Thermodynamic specialization.} VSTF itself imposes no universal relation between accepted-event counts and work, heat, entropy production, or free-energy change. For a particular physical implementation, an independently derived ensemble thermodynamic relation may be imposed on the event-generating subsystem. Likewise, a sample-level upper bound on \VSTE{} exists only if every accepted event admits an independently justified positive resource lower bound $E_{\min,k}^{\mathcal{B}}$, additive over non-overlapping resource segments. In that case $E_{\mathrm{sup}}^{\mathcal{B}}[a,b]\geq\sum_{k\in\mathcal{A}}E_{\min,k}^{\mathcal{B}}$ and $\widehat{\VSTE}_{[a,b]}\leq1/\bar{E}_{\min,\mathcal{A}}^{\mathcal{B}}$. Substitution of $E_{\min,k}^{\mathcal{B}}$ by a free-energy quantity requires a separate physical derivation; it is not implied by VSTF. When such a substitution is proposed, the manuscript or protocol must specify whether $\Delta F_k$ denotes an equilibrium Helmholtz/Gibbs free-energy difference between declared endpoints or a nonequilibrium free-energy functional.
\end{quote}

\subsection{Stochastic Thermodynamics}

Entropy production is a trajectory-level functional in stochastic thermodynamics \cite{seifert2005,seifert2012,barato2015}. VSTF introduces an additional event predicate. Entropy production may occur without a valid transition, and a valid transition may accumulate entropy across many microscopic trajectory segments. Therefore, in general,
\[
  N_{\VST}\neq F(\Sigma_{\mathrm{tot}})
\]
for any universal scalar function $F$.

\begin{observation}[Non-reducibility to dissipation alone]
Two protocols can have equal supplied energy and equal total entropy production but different accepted-transition counts:
\[
  E^{(1)}_{\mathrm{sup}}=E^{(2)}_{\mathrm{sup}},\qquad
  \Sigma^{(1)}_{\mathrm{tot}}=\Sigma^{(2)}_{\mathrm{tot}},\qquad
  N^{(1)}_{\VST}\neq N^{(2)}_{\VST}.
\]
Hence VST observables are not reducible to entropy production alone.
\end{observation}

For example, consider a batch protocol with $m$ candidate attempts, equal energy $E_0$ and equal entropy production $\sigma_0$ per attempt. Protocol 1 produces $m$ localized outputs but only $r_1$ pass distinguishability, persistence, and domain-validity predicates. Protocol 2 uses the same number of attempts and the same per-attempt thermodynamic ledger but produces $r_2\neq r_1$ accepted outputs because its readout or stabilization procedure has a different failure pattern. Then $E_{\mathrm{sup}}^{(1)}=E_{\mathrm{sup}}^{(2)}=mE_0$ and $\Sigma_{\mathrm{tot}}^{(1)}=\Sigma_{\mathrm{tot}}^{(2)}=m\sigma_0$, while $N_{\VST}^{(1)}=r_1$ and $N_{\VST}^{(2)}=r_2$. The difference lies in operational acceptance, not in total dissipation.

\subsection{Critical Transitions and Resilience}

Critical-transition theory studies loss of resilience, slowing of recovery, autocorrelation, variance changes, and regime shifts \cite{kramers1940,scheffer2009,scheffer2018}. VSTF is complementary. It does not merely ask whether a system approaches a transition; it asks when a transition has become valid under a declared operational outcome rule.

\section{Toy Model and Simulation Standard}

Consider a two-basin stochastic process with origin basin $C_0$, target basin $C_1$, and an observed scalar signal
\[
  Y_t = X_t+\eta_t,
  \qquad
  \eta_t\sim \mathcal{N}(0,\sigma^2).
\]
Raw activity, crossing events, committed events, retained events, and state-valid VST events are distinct objects. In the benchmark below,
\[
  \mathcal{E}_{\mathrm{sample+}}
  =
  \{(r,t):Y_{r,t}\geq \theta_{\mathrm{raw}}\}
\]
is the threshold-positive sample set, and $N_{\mathrm{sample+}}=|\mathcal{E}_{\mathrm{sample+}}|$ counts threshold-positive samples or primitive activations. This is an occupancy-like activity measure, not a transition-event set. The event universe is instead
\[
  \mathcal{U}=\{e_1,\ldots,e_n\},
\]
where each $e_k$ is a hysteresis-separated threshold-entry excursion with a start, commitment candidate, and end or censoring status. Within this common event universe,
\[
  \mathcal{E}_{\mathrm{crossing}}
\]
is the set of distinct threshold-entry excursions after hysteresis re-arming. A committed event is a crossing whose filtering posterior exceeds the commitment threshold. A retained event passes lagged validation and first-exit persistence. A state-valid VST event additionally passes the domain predicate. Accounting quality is not part of $A_k$; it is required only for \VSTE{} and other cost-normalized claims.

If these categories are implemented as nested filters of the same candidate-event universe,
\[
  \mathcal{E}_{\VST}
  \subseteq
  \mathcal{E}_{\mathrm{retained}}
  \subseteq
  \mathcal{E}_{\mathrm{committed}}
  \subseteq
  \mathcal{E}_{\mathrm{crossing}}
  \subseteq
  \mathcal{U},
\]
then the ordering
\[
  N_{\mathrm{crossing}}
  \geq
  N_{\mathrm{committed}}
  \geq
  N_{\mathrm{retained}}
  \geq
  N_{\VST}.
\]

\begin{lemma}[Nested event-count monotonicity]
If $\mathcal{E}_{\VST}\subseteq\mathcal{E}_{\mathrm{retained}}\subseteq\mathcal{E}_{\mathrm{committed}}\subseteq\mathcal{E}_{\mathrm{crossing}}$ are nested event sets generated by the same locked candidate-event universe, then
\[
  N_{\VST}\leq N_{\mathrm{retained}}\leq N_{\mathrm{committed}}\leq N_{\mathrm{crossing}}.
\]
\end{lemma}

\begin{proof}
The result follows directly from finite set inclusion and cardinality monotonicity.
\end{proof}

The sample count $N_{\mathrm{sample+}}$ may be reported as a separate raw-activity baseline, but it is not the same type of object as a transition event. A simulation does not prove the inclusion ordering; it estimates the size of attrition, false acceptance, false rejection, and ranking change under noise, retention failure, and cost accounting.

\begin{lemma}[Sample-level resource bound]
If every accepted event $k\in\mathcal{A}$ admits an independently justified positive sample-level resource lower bound $E_{\min,k}^{\mathcal{B}}>0$, and resource contributions are additive over non-overlapping segments inside $\mathcal{B}$, then
\[
  E_{\mathrm{sup}}^{\mathcal{B}}\geq\sum_{k\in\mathcal{A}}E_{\min,k}^{\mathcal{B}},
  \qquad
  \widehat{\VSTE}\leq
  \frac{N_{\VST}}{\sum_{k\in\mathcal{A}}E_{\min,k}^{\mathcal{B}}}
  =
  \frac{1}{\bar E_{\min,\mathcal{A}}^{\mathcal{B}}}.
\]
\end{lemma}

\begin{proof}
The first inequality is the assumed additive lower bound. Dividing the accepted-event count by the larger full-boundary denominator yields the \VSTE{} upper bound.
\end{proof}

A minimal simulation benchmark should therefore report:
\begin{enumerate}
  \item at least 10,000 simulated trajectories from a two-state or multi-state hidden Markov process,
  \item repeated independent seeds or batches, not only one fixed realization,
  \item ground-truth latent state and observation noise,
  \item the filtering posterior $\pi_t(C_j)$ and the classifier-based alternative if used,
  \item raw threshold crossings,
  \item committed first-passage transitions,
  \item retained transitions after first-exit testing,
  \item accepted VST events after domain predicates,
  \item full-boundary costs, rejected attempts, and $Q_{\mathcal{B}}^{\mathrm{acct}}$,
  \item discordant ranking pairs between raw activity, crossings, retained events, and \VSTE{},
  \item false-acceptance and false-rejection rates against latent ground truth rather than against the VSTF decision rule itself,
  \item sensitivity to $\epsilon$, $\alpha$, $\delta$, $\tau_\ast$, hysteresis, and refractory time.
\end{enumerate}

The purpose of this benchmark is not to prove domain truth, but to show when the VST filter changes rankings or conclusions relative to simpler activity, crossing, and first-passage metrics. The locked simulation parameters are listed in Table~\ref{tab:hmm-parameters}.

\section{Numerical Demonstration: Multi-Seed HMM Benchmark}

To demonstrate incremental utility, a two-system hidden-Markov benchmark was implemented with 30 independent batches and 10,000 trajectories per system per batch. Both systems generate a latent two-state process, noisy observations, threshold-positive samples, hysteresis-separated crossing events, posterior commitment events, validation at a lag, retention over a fixed window, operational domain adjudication, latent-ground-truth validity, rejected events, and full-boundary cost. The synthetic domain predicate is intentionally nonmechanistic and implemented as a locked Bernoulli gate. It demonstrates the arithmetic effect of an independent domain-validity layer; it is not a model of biological or clinical validity. The benchmark is not intended as a physical, clinical, or biological validation; it is a controlled proof-of-use showing that the VSTF acceptance layer can change the scientific ranking.

\begin{table}[h]
\centering
\caption{Locked HMM benchmark parameters. The same event rules are applied to both systems.}
\label{tab:hmm-parameters}
\begin{tabular}{lcc}
\toprule
Parameter & System A & System B \\
\midrule
$N_{\mathrm{batches}}$ & 30 & 30 \\
Trajectories per batch & 10,000 & 10,000 \\
Trajectory length $T$ & 72 & 72 \\
$P(X_{t+1}=1\mid X_t=0)$ & 0.125 & 0.024 \\
$P(X_{t+1}=0\mid X_t=1)$ & 0.225 & 0.048 \\
Observation means $(\mu_0,\mu_1)$ & $(0,1)$ & $(0,1)$ \\
Observation noise $\sigma$ & 0.44 & 0.27 \\
Raw threshold $\theta_{\mathrm{raw}}$ & 0.72 & 0.72 \\
Hysteresis re-arm threshold & 0.35 & 0.35 \\
Commitment threshold $\gamma_{\mathrm{gen}}$ & 0.65 & 0.65 \\
Validation threshold $\gamma_{\mathrm{val}}$ & 0.80 & 0.80 \\
Validation lag $\ell$ & 2 & 2 \\
Retention window $\tau_\ast$ & 6 & 6 \\
Retention posterior floor & 0.55 & 0.55 \\
Domain pass probability & 0.72 & 0.88 \\
Base energy per trajectory & 92.0 & 88.0 \\
Energy per crossing & 1.15 & 0.95 \\
Energy per committed event & 2.30 & 1.45 \\
Energy per resolved validation/readout attempt & 3.20 & 2.10 \\
Master random seed & 20260917 & 20260917 \\
\bottomrule
\end{tabular}
\end{table}

\begin{figure}[h]
\centering
\VSTFEventCascadeFigure
\caption{VSTF event-cascade diagram. A dynamic signal first generates a candidate event. The event is countable only if the protocol-level measurement system is distinguishable, the event is localized, retained, domain-admissible, and supported by a complete denominator for the claim being made. Failure at any gate does not erase the observation; it changes the admissible claim and must remain auditable as rejected, unresolved, pending, or proxy evidence.}
\label{fig:event-cascade}
\end{figure}

\begin{figure}[h]
\centering
\VSTFHMMBenchmarkFigure
\caption{Hidden-Markov benchmark showing nested attrition from raw activity to valid VST events, matched-reference error rates, multi-seed ranking robustness, and a small sensitivity sweep. System A produces more raw activity samples, crossing events, and committed events in aggregate, but not more raw activity per full-boundary supplied energy. System B produces fewer early-stage events yet substantially more retained and valid VST events and higher \VSTE{}.}
\label{fig:hmm-benchmark}
\end{figure}

\begin{table}[h]
\centering
\caption{Aggregate summary over 30 independent batches, each with 10,000 trajectories per system.}
\label{tab:hmm-summary}
\begin{tabular}{lrr}
\toprule
Quantity & System A & System B \\
\midrule
Raw activity samples & 6,190,813 & 4,999,028 \\
Crossing events & 2,338,552 & 466,134 \\
Committed events & 1,563,423 & 393,575 \\
Retained events & 105,241 & 217,484 \\
Valid VST events & 75,677 & 191,463 \\
Resolved committed events failing downstream VST criteria & 1,312,764 & 160,062 \\
Latent-ground-truth valid events & 124,952 & 198,042 \\
True positives $(A_k=1,Z_k=1)$ & 46,641 & 166,962 \\
False acceptances & 29,036 & 24,501 \\
False rejections & 78,311 & 31,080 \\
Pending/right-censored events & 174,982 & 42,050 \\
Full-boundary energy & 38,328,212 & 28,151,714 \\
Raw activity per energy & 0.16152 & 0.17757 \\
Crossing events per energy & 0.06101 & 0.01656 \\
Committed events per energy & 0.04079 & 0.01398 \\
Retained events per energy & 0.00275 & 0.00773 \\
Valid VST per energy & 0.00197 & 0.00680 \\
Candidate yield $N_{\VST}/N_{\mathrm{crossing}}$ & 0.032 & 0.410 \\
Retention yield & 0.067 & 0.552 \\
Downstream-rejection fraction, committed denominator & 0.840 & 0.407 \\
Downstream-rejection fraction, resolved denominator & 0.945 & 0.456 \\
Positive predictive value $TP/(TP+FP)$ & 0.616 & 0.872 \\
Sensitivity $TP/(TP+FN)$ & 0.373 & 0.843 \\
False discovery fraction $FP/(TP+FP)$ & 0.384 & 0.128 \\
\bottomrule
\end{tabular}
\end{table}

\begin{table}[h]
\centering
\caption{Multi-seed robustness and sensitivity summary.}
\label{tab:hmm-robustness}
\begin{tabular}{lr}
\toprule
Quantity & Value \\
\midrule
Independent batches & 30 \\
Trajectories per batch per system & 10,000 \\
$\Pr(R_A>R_B\ \mathrm{and}\ \VSTE_B>\VSTE_A)$ & 0.00 \\
$\Pr(R_{\mathrm{cross},A}>R_{\mathrm{cross},B}\ \mathrm{and}\ \VSTE_B>\VSTE_A)$ & 1.00 \\
$\Pr(R_{\mathrm{commit},A}>R_{\mathrm{commit},B}\ \mathrm{and}\ \VSTE_B>\VSTE_A)$ & 1.00 \\
$\Pr(R_{\mathrm{retained},A}>R_{\mathrm{retained},B}\ \mathrm{and}\ \VSTE_B>\VSTE_A)$ & 0.00 \\
Mean $\VSTE_B/\VSTE_A$ & 3.45 \\
Mean raw-efficiency ratio $R_A/R_B$ & 0.910 \\
System A mean \VSTE{} Monte Carlo 95\% interval & [0.00196, 0.00199] \\
System B mean \VSTE{} Monte Carlo 95\% interval & [0.00677, 0.00683] \\
Sensitivity reversal probability, $\sigma$ factor 0.75 & 0.00 across $\tau_\ast\in\{4,6,10\}$ \\
Sensitivity raw-to-\VSTE{} reversal probability, $\sigma$ factor 1.00 & 0.00 across $\tau_\ast\in\{4,6,10\}$ \\
Sensitivity raw-to-\VSTE{} reversal probability, $\sigma$ factor 1.25 & 0.43, 0.53, 0.83 for $\tau_\ast=4,6,10$ \\
\bottomrule
\end{tabular}
\end{table}

\begin{table}[h]
\centering
\caption{Controlled A/B ablations. Each row changes only the listed parameter group in System B relative to System A, except the combined and equal-domain rows.}
\label{tab:hmm-ablations}
\small
\begin{adjustbox}{max width=\linewidth}
\begin{tabular}{lrrrr}
\toprule
Ablation & Raw$\to$\VSTE{} & Crossing$\to$\VSTE{} & Retained$\to$\VSTE{} & Mean $\VSTE_B/\VSTE_A$ \\
\midrule
Dynamics only & 0.75 & 1.00 & 0.00 & 2.58 \\
Noise only & 0.00 & 1.00 & 0.00 & 1.73 \\
Domain only & 0.38 & 0.63 & 0.50 & 1.22 \\
Cost only & 0.00 & 0.00 & 0.00 & 1.13 \\
Domain+cost only & 0.00 & 0.00 & 0.00 & 1.39 \\
Retention-challenge & 0.00 & 0.00 & 0.13 & 1.41 \\
Combined with equal domain probability & 0.00 & 1.00 & 0.00 & 2.80 \\
Combined & 0.00 & 1.00 & 0.00 & 3.42 \\
\bottomrule
\end{tabular}
\end{adjustbox}
\end{table}

Under the corrected operation-based energy ledger, System A has more raw activity samples in aggregate, but it no longer has higher raw activity per unit full-boundary energy. This is intentional: the denominator charges validation/readout attempts as operations actually performed rather than as a retrospective function of the final VST label. The strongest remaining ranking contrast is therefore not raw-per-energy but event-level attrition: crossing and committed-event baselines rank System A above System B in every batch, while \VSTE{} ranks System B above System A. The retained-event baseline already ranks System B above System A in the main locked operating point, which is expected because retention is one of the core VSTF filters; this makes clear that the benchmark does not claim VSTF defeats every simpler event-level metric. The sensitivity table is deliberately reported in its current form: the raw-to-\VSTE{} reversal is absent at $\sigma=1.00$ and appears only in one high-noise, long-retention cell at $\sigma=1.25,\tau_\ast=10$ with three sensitivity batches per cell. Therefore the sensitivity sweep should be read as a small diagnostic stress test, not as a precise probability estimate. The VSTF claim is strongest in regimes where noise, retention failure, domain filtering, and cost accounting create a measurable separation between activity and retained domain-valid outcome. The accompanying random-parameter stress file samples eight additional A/B pairs over transition, noise, domain-validity, and energy-ledger ranges; it is a broader robustness check, not a substitute for a full domain-specific empirical benchmark. The reported intervals are Monte Carlo intervals across synthetic batches, not confidence intervals for a physical population parameter.

\begin{table}[h]
\centering
\caption{Additional scenario stress tests. Correlated-domain scenarios make domain validity depend on latent retention and posterior evidence; HSMM scenarios replace geometric HMM dwell times with explicit duration sampling.}
\label{tab:hmm-scenarios}
\small
\begin{adjustbox}{max width=\linewidth}
\begin{tabular}{lrrrrr}
\toprule
Scenario & Raw$\to$\VSTE{} & Crossing$\to$\VSTE{} & Retained$\to$\VSTE{} & Mean $\VSTE_B/\VSTE_A$ & PPV B \\
\midrule
HMM, independent domain & 0.00 & 1.00 & 0.00 & 3.45 & 0.871 \\
HMM, correlated domain & 0.00 & 1.00 & 0.00 & 2.98 & 0.954 \\
HSMM, independent domain & 0.00 & 1.00 & 0.00 & 6.53 & 0.876 \\
HSMM, correlated domain & 0.00 & 1.00 & 0.00 & 5.69 & 0.957 \\
\bottomrule
\end{tabular}
\end{adjustbox}
\end{table}

Table~\ref{tab:hmm-scenarios} addresses two structural limitations of the main didactic benchmark. First, the domain predicate need not be independent of the observed event packet; correlated-domain variants preserve the ranking reversal after dependence is introduced. Second, retention is not tied to geometric HMM dwell times; HSMM variants with explicit sampled durations preserve the qualitative result. The scenario table is deliberately small and diagnostic rather than confirmatory. Its purpose is to show that the demonstration is not solely an artifact of an independent Bernoulli domain gate or a geometric HMM duration assumption.

\begin{table}[h]
\centering
\caption{Model-misspecification stress test. Each row uses four independent batches with 3,000 trajectories per system. The inference layer remains the locked Gaussian HMM filter while the data-generating or reference process is perturbed. These are Monte Carlo stress summaries, not population confidence intervals.}
\label{tab:model-misspecification}
\small
\begin{adjustbox}{max width=\linewidth}
\begin{tabular}{lrrrrr}
\toprule
Scenario & Crossing$\to$\VSTE{} & Retained$\to$\VSTE{} & Mean $\VSTE_B/\VSTE_A$ & PPV B & TPR B \\
\midrule
Matched HMM/Gaussian reference & 1.00 & 0.00 & 3.42 & 0.872 & 0.836 \\
HSMM truth, HMM filter & 1.00 & 0.00 & 6.56 & 0.876 & 0.843 \\
Student-$t$ observation noise & 1.00 & 0.00 & 3.06 & 0.874 & 0.815 \\
Time-drifting transition rates & 1.00 & 0.00 & 3.38 & 0.875 & 0.842 \\
5\% reference-label error & 1.00 & 0.00 & 3.48 & 0.834 & 0.815 \\
\bottomrule
\end{tabular}
\end{adjustbox}
\end{table}

The misspecification stress suite in Table~\ref{tab:model-misspecification} is intentionally separate from the main benchmark. It asks whether the qualitative ranking contrast survives when the locked inference model is wrong in controlled ways: non-geometric dwell times, heavy-tailed observations, time-varying transition probabilities, or a fallible reference label. The answer in this synthetic design is yes for the crossing-to-\VSTE{} contrast, but not because VSTF is immune to misspecification. Predictive values and sensitivities move, especially under reference-label error, so domain studies must still run model checks, calibration checks, and reference-standard sensitivity analyses rather than citing the synthetic benchmark as empirical validation.

\begin{table}[h]
\centering
\caption{Sampling-resolution stress test. The factor is relative to the base grid $\Delta t_0$; smaller factors are finer sampling. Relative accepted counts are normalized to the $\Delta t_0$ row.}
\label{tab:hmm-sampling}
\small
\begin{adjustbox}{max width=\linewidth}
\begin{tabular}{lrrrrrr}
\toprule
$\Delta t/\Delta t_0$ & Steps & Crossing$\to$\VSTE{} & Mean $\VSTE_B/\VSTE_A$ & Rel. $N_{\VST,A}$ & Rel. $N_{\VST,B}$ & PPV B \\
\midrule
0.25 & 288 & 1.00 & 10.53 & 0.54 & 1.26 & 0.881 \\
0.50 & 144 & 1.00 & 5.11 & 0.82 & 1.11 & 0.874 \\
1.00 & 72 & 1.00 & 3.36 & 1.00 & 1.00 & 0.871 \\
2.00 & 36 & 1.00 & 2.69 & 1.06 & 0.92 & 0.865 \\
4.00 & 18 & 1.00 & 3.80 & 0.51 & 0.66 & 0.854 \\
\bottomrule
\end{tabular}
\end{adjustbox}
\end{table}

The sampling-resolution stress test shows ranking robustness more strongly than count invariance. Accepted-event counts are grid-sensitive, especially at very fine or coarse observation grids. This is not hidden: VSTF claims based on event counts must either show admissible stability over a declared sampling neighborhood or explicitly report the conclusion as resolution-dependent. The factor-one row in Table~\ref{tab:hmm-sampling} is generated inside the sampling-resolution stress stream with two batches per grid cell, not by reusing the 30-batch main Monte Carlo stream; this is why its mean \VSTE{} ratio is close to, but not identical with, Table~\ref{tab:hmm-robustness}.

\begin{table}[h]
\centering
\caption{Many-system rank-discordance stress test. Thirty synthetic systems were generated from pre-specified stratified parameter ranges and evaluated over five independent seeds with 1,500 trajectories per seed. Discordance is the fraction of comparable system pairs for which the baseline ranking disagrees with \VSTE{} ranking; intervals bootstrap systems, not pairwise comparisons.}
\label{tab:many-system-discordance}
\small
\begin{adjustbox}{max width=\linewidth}
\begin{tabular}{lrrr}
\toprule
Baseline ranking metric & Discordance vs \VSTE{} & Bootstrap low & Bootstrap high \\
\midrule
Raw activity per energy & 0.138 & 0.062 & 0.220 \\
Crossing events per energy & 0.552 & 0.423 & 0.695 \\
Committed events per energy & 0.382 & 0.264 & 0.500 \\
Retained events per energy & 0.057 & 0.022 & 0.107 \\
Retention yield & 0.166 & 0.096 & 0.251 \\
Validity yield & 0.554 & 0.428 & 0.676 \\
\bottomrule
\end{tabular}
\end{adjustbox}
\end{table}

\begin{figure}[h]
\centering
\VSTFRankFigure
\caption{Many-system rank-displacement diagnostic. Each point is one synthetic system. The x-axis ranks systems by retained-event efficiency and the y-axis ranks the same systems by \VSTE{}. Points near the diagonal indicate that retention already explains most VSTE ranking; displaced points indicate additional cost/domain/accounting effects.}
\label{fig:many-system-rank}
\end{figure}

The many-system benchmark directly tests the nearest-baseline concern raised by the review. Table~\ref{tab:many-system-discordance} reports pairwise rank discordance against \VSTE{}, and Figure~\ref{fig:many-system-rank} visualizes the retained-efficiency versus \VSTE{} rank displacement. The benchmark confirms that crossing and committed-event baselines can be strongly discordant with \VSTE{} across a population of systems. It also shows that retained-event efficiency is a much closer baseline in this synthetic design, with only 0.057 pairwise discordance against \VSTE{}. Therefore the benchmark supports the modest claim that VSTF can substantially change rankings relative to raw, crossing, and committed activity, while the incremental value beyond a retention-only baseline is parameter-regime dependent and should be tested explicitly in each domain.

\section{Reproducibility Materials}

The synthetic benchmark is intended to be fully reproducible. The required companion files are:
\begin{itemize}
  \item \texttt{reproduce\_all.py}: single-command workflow that regenerates the synthetic benchmark, real-data component audits, unified real-calculation ledger, and checksum manifest.
  \item \texttt{vstf\_hmm\_benchmark.py}: deterministic simulation script with fixed seed, model parameters, event filters, and figure generation.
  \item \texttt{vstf\_hmm\_benchmark\_summary.csv}: aggregate benchmark output used in Table~\ref{tab:hmm-summary}.
  \item \texttt{vstf\_hmm\_benchmark\_monte\_carlo.csv}: multi-seed robustness summary used in Table~\ref{tab:hmm-robustness}.
  \item \texttt{vstf\_hmm\_benchmark\_batch\_level.csv}: all 30 HMM batch-level results for both benchmark systems.
  \item \texttt{vstf\_hmm\_benchmark\_sensitivity.csv}: sensitivity sweep over observation noise and retention window.
  \item \texttt{vstf\_hmm\_sampling\_resolution.csv}: sampling-grid stress test used in Table~\ref{tab:hmm-sampling}.
  \item \texttt{vstf\_hmm\_benchmark\_ablations.csv}: controlled A/B ablation summary used in Table~\ref{tab:hmm-ablations}.
  \item \texttt{vstf\_hmm\_random\_parameter\_stress.csv}: random-parameter A/B stress test.
  \item \texttt{vstf\_hmm\_scenario\_stress.csv}: correlated-domain and HSMM scenario stress tests.
  \item \texttt{vstf\_model\_misspecification\_stress.csv}: model-misspecification and reference-error stress tests.
  \item \texttt{vstf\_many\_system\_benchmark.csv}: many-system synthetic summary.
  \item \texttt{vstf\_many\_system\_discordance.csv}: pairwise rank-discordance table.
  \item \texttt{vstf\_hmm\_event\_sample.csv}: machine-readable event-level audit sample.
  \item \texttt{vstf\_real\_datasets\_validation.py}: locked multi-dataset real-data acceptance audit.
  \item \texttt{vstf\_real\_datasets\_summary.csv}: aggregate real-data classification audit output used in Table~\ref{tab:real-static-datasets}.
  \item \texttt{vstf\_real\_datasets\_folds.csv}: fold-level real-data classification audit output.
  \item \texttt{vstf\_real\_component\_validation.py}: real-data component audit for temporal retention, first-exit, cost-normalized \VSTE{}, and intervention effects.
  \item \texttt{vstf\_real\_component\_validation\_summary.csv}: component-level real-data audit used in Table~\ref{tab:real-component-datasets}.
  \item \texttt{vstf\_household\_power\_events.csv}: event-level ledger for the real household-power time-series audit used in Table~\ref{tab:power-ledger}.
  \item \texttt{vstf\_lalonde\_treatment\_effect.csv}: treatment-effect ledger used in Table~\ref{tab:lalonde-ledger}.
  \item \texttt{vstf\_full\_real\_calculation\_ledger.csv}: unified real-calculation ledger for all real-data components.
  \item \texttt{vstf\_event\_cascade.pdf}: optional exported workflow diagram matching Figure~\ref{fig:event-cascade}.
  \item \texttt{vstf\_hmm\_benchmark.pdf}: optional exported benchmark graphic matching Figure~\ref{fig:hmm-benchmark}.
  \item \texttt{vstf\_many\_system\_rank\_displacement.pdf}: optional exported rank diagnostic matching Figure~\ref{fig:many-system-rank}.
  \item \texttt{EVIDENCE\_MANIFEST.sha256}: SHA-256 manifest tying the manuscript source, scripts, generated CSV/PDF outputs, README, reproducibility notes, and pinned requirements to one evidence package.
  \item \texttt{FULL\_VALIDATION\_PROTOCOL.md}: domain-level checklist for any future claim that VSTF has been fully validated rather than only component-audited.
  \item \texttt{README.md}, \texttt{REPRODUCIBILITY.md}, and \texttt{requirements.txt}: package index, seed policy, preprocessing notes, one-command reproduction instructions, interpretation guardrails, and pinned minimal dependencies.
\end{itemize}
The current public repository is the \href{https://github.com/MartinPetrasek123/R_Universe_Evidence_Audited/tree/main/vstf}{VSTF reproducibility package}; a submission archive should cite an immutable release DOI or exact commit hash. The command \texttt{python3 reproduce\_all.py} should be run from the package root before release; it fails if the sensitivity table regenerated from CSV no longer matches the manuscript. The benchmark does not require domain data and is not evidence for a biological or clinical claim; it is a reproducible methodological demonstration that the VSTF acceptance layer can alter ranking and false-success interpretation.

\section{Verification and Validation Status}

This manuscript separates \emph{verification} from \emph{validation}. Verification asks whether the definitions, accounting rules, code, tables, and synthetic examples are internally reproducible and logically consistent. Validation asks whether the framework improves inference, prediction, decision utility, or intervention assessment in a real domain with locked data, external endpoints, and appropriate ethical approval. Table~\ref{tab:verification-status} states the status of each claim layer.

\begin{table}[h]
\centering
\caption{Verification and validation status of the present manuscript.}
\label{tab:verification-status}
\small
\begin{adjustbox}{max width=\linewidth}
\begin{tabular}{p{0.24\linewidth}p{0.34\linewidth}p{0.32\linewidth}}
\toprule
Claim layer & What is verified here & What remains to be validated \\
\midrule
Formal definitions & The event predicates, status labels, matched-reference notation, denominators, and claim restrictions are defined explicitly and used consistently across the manuscript. & Domain-specific state variables, thresholds, and admissible predicates must be locked before each empirical study. \\
Synthetic benchmark & The HMM benchmark, ablations, sampling-resolution stress test, scenario stress test, model-misspecification stress test, and many-system rank-discordance stress test are reproducible from the companion script and fixed seed. & The synthetic benchmark is not evidence that VSTF improves any clinical, biological, or hardware system in practice. \\
Numerical conclusions & The synthetic results verify that the VSTF acceptance layer can change ranking relative to raw, crossing, and committed-event baselines under declared assumptions. & External datasets must test whether such ranking changes persist in representative real systems. \\
Measurement validity & The manuscript specifies localization, distinguishability, retention, domain validity, reference matching, censoring, and denominator completeness requirements. & Each real application must estimate measurement error, calibration, reference-standard imperfection, and uncertainty using domain data. \\
Clinical or biological utility & No clinical, cancer, neurorehabilitation, or animal-study benefit is claimed from the synthetic demonstration. & Utility, causal benefit, safety, fairness, and generalization require prospective or externally validated domain-specific studies. \\
\bottomrule
\end{tabular}
\end{adjustbox}
\end{table}

Accordingly, the present article should be cited as a methodological and reproducibility-checked framework paper with a limited public real-data verification audit. It is not a completed clinical validation study. A domain paper using VSTF must provide its own locked protocol, dataset provenance, ethics statement, endpoint adjudication, baseline comparison, uncertainty analysis, and falsification criteria before making substantive applied claims.

\section{Real-Data Verification Audit}

To prevent the framework from resting only on synthetic demonstrations, the real-data audit was expanded to cover each measurable VSTF component. Static classification datasets are used only for the acceptance/adjudication layer. A real minute-level electricity time series is used for temporal segmentation, dwell, first-exit retention, and cost-normalized \VSTE{}. A real treatment dataset is used for the intervention-effect layer. The datasets are public and directly linked in Tables~\ref{tab:real-static-datasets} and \ref{tab:real-component-datasets}. These calculations verify operational behavior on real data; they do not establish clinical utility, safety, deployment readiness, or generalization beyond the audited datasets.

All static classification audits used the same locked rule: five stratified folds, master seed 20260919, feature standardization estimated inside the training fold only, shrinkage linear discriminant analysis with shrinkage 0.10, raw decision threshold $p\geq0.5$, and VSTF acceptance only when $p\leq0.02$ or $p\geq0.98$. Cases inside the indeterminate band are pending and are not counted as accepted successes. Table~\ref{tab:real-static-datasets} gives the real-data classification ledger.

\begin{table}[h]
\centering
\caption{Real-data acceptance-layer audit across five public datasets. Dataset names are clickable source links. The accepted-accuracy column is computed as accepted true-reference decisions divided by all accepted decisions.}
\label{tab:real-static-datasets}
\scriptsize
\begin{adjustbox}{max width=\linewidth}
\begin{tabular}{p{0.30\linewidth}rrrrrr}
\toprule
Dataset & $n$ & Raw acc. & Accepted & Pending & Accepted acc. & Coverage \\
\midrule
\href{https://archive.ics.uci.edu/ml/machine-learning-databases/breast-cancer-wisconsin/wdbc.data}{Wisconsin Diagnostic Breast Cancer} & 569 & 0.9526 & 446 & 123 & 443/446 = 0.9933 & 446/569 = 0.7838 \\
\href{https://archive.ics.uci.edu/ml/machine-learning-databases/ionosphere/ionosphere.data}{Johns Hopkins Ionosphere} & 351 & 0.8775 & 192 & 159 & 185/192 = 0.9635 & 192/351 = 0.5470 \\
\href{https://archive.ics.uci.edu/ml/machine-learning-databases/undocumented/connectionist-bench/sonar/sonar.all-data}{Connectionist Bench Sonar} & 208 & 0.7684 & 48 & 160 & 43/48 = 0.8958 & 48/208 = 0.2308 \\
\href{https://archive.ics.uci.edu/ml/machine-learning-databases/heart-disease/processed.cleveland.data}{Cleveland Heart Disease} & 297 & 0.8483 & 73 & 224 & 72/73 = 0.9863 & 73/297 = 0.2458 \\
\href{https://archive.ics.uci.edu/ml/machine-learning-databases/haberman/haberman.data}{Haberman Survival} & 306 & 0.7519 & 0 & 306 & 0/0, no accepted cases & 0/306 = 0.0000 \\
\bottomrule
\end{tabular}
\end{adjustbox}
\end{table}

Table~\ref{tab:real-static-datasets} confirms the acceptance-layer arithmetic on real datasets. For example, in WDBC, $446$ of $569$ cases are accepted, $123$ are pending, and accepted reference accuracy is $443/(443+3)=443/446=0.9933$. In Ionosphere, accepted reference accuracy is $185/(185+7)=185/192=0.9635$. In Sonar, it is $43/(43+5)=43/48=0.8958$. In Cleveland Heart Disease, it is $72/(72+1)=72/73=0.9863$. Haberman is a negative real-data result: under the locked conservative threshold, no case is sufficiently certain to enter the accepted numerator, so the correct VSTF output is complete pending status rather than a forced success claim.

Because this component is close to selective classification, Table~\ref{tab:real-static-risk-calibration} reports ordinary risk--coverage and calibration diagnostics for the same held-out fold predictions. The VSTF acceptance gate is therefore not presented as a substitute for reject-option evaluation. It is a stricter event-admissibility wrapper around an ordinary probabilistic classifier, and its probabilities still require Brier, ECE, and coverage-conditioned risk reporting.

\begin{table}[h]
\centering
\caption{Held-out calibration and selective-risk diagnostics for the real-data classification audit. AURC is the mean selective risk obtained by accepting cases in descending confidence order.}
\label{tab:real-static-risk-calibration}
\scriptsize
\begin{adjustbox}{max width=\linewidth}
\begin{tabular}{p{0.30\linewidth}rrrrr}
\toprule
Dataset & Brier & ECE$_{10}$ & AURC & Risk at 50\% cov. & Risk at 80\% cov. \\
\midrule
\href{https://archive.ics.uci.edu/ml/machine-learning-databases/breast-cancer-wisconsin/wdbc.data}{Wisconsin Diagnostic Breast Cancer} & 0.0324 & 0.0133 & 0.0057 & 0.0035 & 0.0088 \\
\href{https://archive.ics.uci.edu/ml/machine-learning-databases/ionosphere/ionosphere.data}{Johns Hopkins Ionosphere} & 0.0984 & 0.0567 & 0.0508 & 0.0341 & 0.0712 \\
\href{https://archive.ics.uci.edu/ml/machine-learning-databases/undocumented/connectionist-bench/sonar/sonar.all-data}{Connectionist Bench Sonar} & 0.1679 & 0.0883 & 0.1259 & 0.1154 & 0.1796 \\
\href{https://archive.ics.uci.edu/ml/machine-learning-databases/heart-disease/processed.cleveland.data}{Cleveland Heart Disease} & 0.1189 & 0.0506 & 0.0632 & 0.0805 & 0.1050 \\
\href{https://archive.ics.uci.edu/ml/machine-learning-databases/haberman/haberman.data}{Haberman Survival} & 0.1811 & 0.0308 & 0.2084 & 0.1634 & 0.2163 \\
\bottomrule
\end{tabular}
\end{adjustbox}
\end{table}

The risk--coverage table changes the interpretation of Table~\ref{tab:real-static-datasets}: high accepted accuracy at low coverage is not by itself a deployment result. It is a selective operating point that must be compared with the full risk--coverage curve and with calibration quality. Haberman remains an intentionally negative result because the locked acceptance threshold accepts no cases despite an apparently ordinary raw accuracy.

The remaining VSTF components require data with time, cost, or treatment/comparison structure. Table~\ref{tab:real-component-datasets} therefore uses two additional real datasets: the UCI Individual Household Electric Power Consumption time series for temporal event segmentation, dwell, first-exit retention, and cost-normalized \VSTE{}; and the Lalonde/MatchIt treated-versus-PSID comparison dataset for a descriptive intervention/comparison calculation. The Lalonde calculation is not interpreted as an unadjusted causal estimate, because the 429 comparison observations are not randomized NSW controls.

\begin{table}[h]
\centering
\caption{Real-data component audit for VSTF elements that cannot be computed from static classification rows. Dataset names are clickable source links.}
\label{tab:real-component-datasets}
\scriptsize
\begin{adjustbox}{max width=\linewidth}
\begin{tabular}{p{0.25\linewidth}p{0.30\linewidth}p{0.32\linewidth}}
\toprule
Component & Real dataset & Computed result \\
\midrule
Temporal event segmentation & \href{https://archive.ics.uci.edu/ml/machine-learning-databases/00235/household_power_consumption.zip}{UCI household electric power consumption} & The complete minute grid contains 2,075,259 slots, of which 2,049,280 are observed and 25,979 are missing. The low-to-high rule detected 372 raw candidate attempts. \\
Dwell rule & \href{https://archive.ics.uci.edu/ml/machine-learning-databases/00235/household_power_consumption.zip}{UCI household electric power consumption} & Dwell was defined as power remaining at or above the 75th percentile for 5 consecutive minutes. \\
First-exit retention & \href{https://archive.ics.uci.edu/ml/machine-learning-databases/00235/household_power_consumption.zip}{UCI household electric power consumption} & 126 raw candidates passed dwell and had resolvable retention windows; 56 remained above the 25th percentile throughout the 30-minute window, so retention fraction $=56/126=0.4444$. \\
Cost-normalized \VSTE{} & \href{https://archive.ics.uci.edu/ml/machine-learning-databases/00235/household_power_consumption.zip}{UCI household electric power consumption} & Observed energy was 37,283.7477 kWh and the missing-adjusted sensitivity denominator was 37,544.4037 kWh; observed-denominator \VSTE{} $=56/37283.7477=0.0015020$ retained events/kWh. \\
Descriptive treatment-comparison mean difference & \href{https://vincentarelbundock.github.io/Rdatasets/csv/MatchIt/lalonde.csv}{Lalonde/MatchIt NSW treated versus PSID comparison data} & Mean 1978 earnings difference $=6349.1435-6984.1697=-635.0262$ USD; 95\% CI $[-1962.3291,\,692.2767]$. \\
Descriptive transition-fraction difference & \href{https://vincentarelbundock.github.io/Rdatasets/csv/MatchIt/lalonde.csv}{Lalonde/MatchIt NSW treated versus PSID comparison data} & $\Pr(\mathrm{re78}>\mathrm{re75}\mid\mathrm{treated})-\Pr(\mathrm{re78}>\mathrm{re75}\mid\mathrm{PSID\ comparison})=0.6703-0.6620=0.0083$. \\
\bottomrule
\end{tabular}
\end{adjustbox}
\end{table}

The household-power calculation is shown explicitly in Table~\ref{tab:power-ledger}. The event rule is a real time-series rule rather than a static classifier: a candidate event starts when the previous minute is at or below the 25th percentile of active power and the current minute is at or above the 75th percentile. The event is retained only if the signal does not first-exit below the 25th percentile during the following 30 minutes. Missing minutes are preserved in the full grid and flagged in \texttt{vstf\_household\_power\_events.csv}; events with missing dwell or retention windows are pending rather than silently counted as successes or failures.

\begin{table}[h]
\centering
\caption{Real household-power temporal and cost ledger.}
\label{tab:power-ledger}
\small
\begin{adjustbox}{max width=\linewidth}
\begin{tabular}{lr}
\toprule
Quantity & Value \\
\midrule
Full minute grid / observed / missing & 2,075,259 / 2,049,280 / 25,979 \\
Missing fraction & $25979/2075259=0.01252$ \\
Time span & 2006-12-16 17:24 to 2010-11-26 21:02 \\
Low threshold, $q_{25}$ & 0.308 kW \\
High threshold, $q_{75}$ & 1.528 kW \\
Raw candidate events & 372 \\
Candidate events after dwell & 126 \\
Accepted retained events & 56 \\
Rejected first-exit events & 70 \\
Rejected dwell-failure events & 246 \\
Pending missing-window events & 0 \\
Retention fraction & $56/126=0.4444$ \\
Observed energy & 37,283.7477 kWh \\
Estimated missing energy & 260.6560 kWh \\
Estimated total energy & 37,544.4037 kWh \\
Accepted-event observed energy & 58.0500 kWh \\
Observed-denominator \VSTE{} & $56/37283.7477=0.0015020$ events/kWh \\
Missing-adjusted sensitivity \VSTE{} & $56/37544.4037=0.0014916$ events/kWh \\
Denominator completeness flag & incomplete, because 25,979 minutes are missing \\
\bottomrule
\end{tabular}
\end{adjustbox}
\end{table}

The treatment/comparison calculation is shown explicitly in Table~\ref{tab:lalonde-ledger}. It is included to keep the intervention layer separate from ordinary classification and temporal event detection. The result is intentionally modest and descriptive: the mean outcome difference is negative and its 95\% interval crosses zero; the positive-transition fraction differs by only 0.0083. Because the comparison group is the PSID comparison sample, not randomized NSW controls, this table must not be cited as a causal treatment-effect estimate without a separate locked matching, weighting, balance, and sensitivity analysis.

\begin{table}[h]
\centering
\caption{Real treated-versus-comparison ledger using the Lalonde/MatchIt dataset. The comparison group consists of PSID comparison observations; the unadjusted differences are descriptive rather than causal.}
\label{tab:lalonde-ledger}
\small
\begin{adjustbox}{max width=\linewidth}
\begin{tabular}{lr}
\toprule
Quantity & Value \\
\midrule
Samples & 614 \\
Treated / PSID comparison & 185 / 429 \\
Treated mean 1978 earnings & 6,349.1435 USD \\
PSID comparison mean 1978 earnings & 6,984.1697 USD \\
Difference in means & $6349.1435-6984.1697=-635.0262$ USD \\
Standard error & 677.1954 USD \\
95\% confidence interval & $[-1962.3291,\,692.2767]$ USD \\
Treated positive transition, $\mathrm{re78}>\mathrm{re75}$ & 0.6703 \\
PSID comparison positive transition, $\mathrm{re78}>\mathrm{re75}$ & 0.6620 \\
Transition risk difference & $0.6703-0.6620=0.0083$ \\
\bottomrule
\end{tabular}
\end{adjustbox}
\end{table}

These real-data audits support only the component-level claims shown in Tables~\ref{tab:real-static-datasets}--\ref{tab:lalonde-ledger}. They verify that the VSTF acceptance layer, pending status, temporal retention gate, first-exit rejection, cost-normalized retained-event denominator, and treatment/comparison distinction can be computed on real public data. They do not prove that VSTF is clinically useful, biologically causal, or superior for any deployment task without an application-specific validation study.

\subsection{Full-Validation Status}

Table~\ref{tab:full-validation-status} states the validation status after the synthetic and real-data audits. The distinction is essential: a component is \emph{computed and reproduced} when the present package regenerates the reported value from public data or deterministic simulation; it is \emph{fully validated} only when the same claim survives an application-specific external or prospective study with locked endpoints, appropriate denominators, and a defensible estimand. Under that stricter standard, the present manuscript is not yet a fully validated clinical, biological, or hardware deployment study. It is a reproducible methodological framework with component-level real-data audits.

\begin{table}[h]
\centering
\caption{Validation status after the present reproducibility and real-data audits.}
\label{tab:full-validation-status}
\scriptsize
\begin{adjustbox}{max width=\linewidth}
\begin{tabular}{p{0.22\linewidth}p{0.28\linewidth}p{0.36\linewidth}}
\toprule
Claim or component & Present status & Requirement for full validation \\
\midrule
Formal VSTF definitions & Internally specified and checked for consistency across the manuscript. & Independent use by external investigators or standards-style comparison with domain-specific endpoint/adjudication frameworks. \\
Synthetic HMM ranking contrast & Reproduced by \texttt{reproduce\_all.py}; main crossing/commit-to-\VSTE{} contrast is supported by deterministic code and CSV outputs. & Broader benchmark families and externally reviewed simulation protocols; probability claims require larger sensitivity grids and binomial intervals. \\
Sensitivity Table 5 & Corrected to match the regenerated 30-batch-per-cell CSV: raw-to-\VSTE{} reversal is absent at $\sigma=1.00$ and increases across the high-noise $\sigma=1.25$ retention cells. & Larger confirmatory sensitivity runs, e.g. at least 100--1000 seeds per cell for publication-grade probability intervals. \\
Acceptance/pending layer & Computed on five public static datasets in Table~\ref{tab:real-static-datasets}. & External task-specific validation showing that accepted/pending decisions improve a declared utility, safety, or scientific endpoint beyond standard baselines. \\
Temporal segmentation and first-exit retention & Computed on the public household-power time series in Tables~\ref{tab:real-component-datasets} and \ref{tab:power-ledger}. & Domain-specific time-series studies with locked state definitions, independent adjudication, and sensitivity to missing data, sampling cadence, and threshold choice. \\
Cost-normalized \VSTE{} & Computed on real household-power energy data as retained events per kWh. & Application-specific cost boundaries, complete denominator ledgers, and comparison against ordinary yield, rate, and efficiency metrics. \\
Treatment/intervention layer & Descriptive Lalonde/MatchIt treated-versus-PSID comparison computed; not interpreted as causal. & Locked causal estimand, treatment/control construction, covariates, matching or weighting, balance diagnostics, standard-error method, and sensitivity to unmeasured confounding. \\
Clinical or biological utility & Not claimed as validated by the present evidence. & Prospective or externally validated domain studies with ethics approval, endpoint governance, calibration, utility analysis, fairness checks, and safety monitoring. \\
\bottomrule
\end{tabular}
\end{adjustbox}
\end{table}

Thus, the strongest defensible statement is not that VSTF is fully empirically validated as a universal scientific instrument. The defensible statement is that the mathematical grammar, the synthetic benchmark, the real-data acceptance layer, the real temporal-retention ledger, the real cost denominator, and the descriptive treatment/comparison separation are all reproducibly computable from the provided package. Full validation remains a domain-specific empirical program.

\subsection{Additional External Component Check}

An additional external component check was performed on public datasets not used in the preceding package-level real-data audit. These calculations are reported only as component checks, not as a claim of complete framework validation. The classification checks used the same locked acceptance rule as above: five stratified folds, training-fold standardization, shrinkage LDA with shrinkage 0.10, raw decision threshold $p\geq0.5$, and accepted decisions only when $p\leq0.02$ or $p\geq0.98$. Table~\ref{tab:external-component-check} reports the results.

\begin{table}[h]
\centering
\caption{Additional external component check on public datasets not used in the main real-data audit. Dataset names are clickable source links.}
\label{tab:external-component-check}
\scriptsize
\begin{adjustbox}{max width=\linewidth}
\begin{tabular}{p{0.30\linewidth}p{0.38\linewidth}p{0.22\linewidth}}
\toprule
Component & External dataset & Computed result \\
\midrule
Acceptance/pending layer & \href{https://archive.ics.uci.edu/ml/machine-learning-databases/00267/data_banknote_authentication.txt}{UCI Banknote Authentication} & Raw accuracy $=0.9767$; accepted accuracy $=1101/1101=1.0000$; coverage $=1101/1372=0.8025$; pending fraction $=271/1372=0.1975$. \\
Acceptance/pending layer & \href{https://archive.ics.uci.edu/ml/machine-learning-databases/spambase/spambase.data}{UCI Spambase} & Raw accuracy $=0.8878$; accepted accuracy $=1379/1404=0.9822$; coverage $=1404/4601=0.3052$; pending fraction $=3197/4601=0.6948$. \\
Temporal retention and cost denominator & \href{https://archive.ics.uci.edu/ml/machine-learning-databases/00374/energydata_complete.csv}{UCI Appliances Energy Prediction} & With a 30-minute dwell and 60-minute retention window, 50 candidate appliance-use events were detected. Under first-exit below $q_{50}$, 46/50 were retained; under a stricter 120-minute window, 34/50 were retained. Total appliance energy was 1928.01 kWh, so the stricter retained-event yield was $34/1928.01=0.0176$ events/kWh. \\
Intervention contrast & \href{https://vincentarelbundock.github.io/Rdatasets/csv/AER/STAR.csv}{Project STAR kindergarten class-size dataset} & Small-class versus regular-class contrast: reading difference $=440.5518-434.7327=5.8191$, 95\% CI $[3.7770,\,7.8612]$; mathematics difference $=491.3901-483.3102=8.0799$, 95\% CI $[4.9615,\,11.1982]$. \\
\bottomrule
\end{tabular}
\end{adjustbox}
\end{table}

Table~\ref{tab:external-component-check} strengthens the component-level evidence: the acceptance gate generalizes to two additional external static datasets, temporal retention and cost-normalized retained-event yield can be computed on a separate energy time series, and an intervention contrast can be computed on an external experimental education dataset. It still does not make the framework completely validated. The reason is structural, not merely computational: no dataset used here contains all required VSTF layers at once.

For a complete validation of VSTF as a full framework, a single locked study would need a time-indexed state trajectory, candidate events, dwell and first-exit retention, a reference endpoint or adjudicated ground truth, accepted/rejected/pending labels, a complete cost or exposure denominator, and, for intervention claims, a randomized or defensibly adjusted treatment/control structure. The present evidence covers these layers across separate public datasets. It therefore validates component computability and supports the methodological claim, but it does not establish one complete end-to-end validation of VSTF in a single clinical, biological, hardware, or intervention domain.

\section{Operational Workflow}

A VSTF study should be executable as the following locked workflow.

\begin{enumerate}
  \item \textbf{Define the scientific claim.} State whether the claim concerns event detection, retained outcome, cost-normalized efficiency, clinical prediction, intervention response, or causal treatment benefit.
  \item \textbf{Declare the state representation.} Specify $X_t$, measured data $Y_t$, preprocessing, feature construction, and the observation model or measurement-error approximation.
  \item \textbf{Freeze state regions.} Define $C_i$ and $C_j$ using prior knowledge or a training/reference cohort. Lock them before confirmatory testing.
  \item \textbf{Freeze event segmentation.} Specify departure, commitment, dwell, exit, return, hysteresis, refractory time, and double-counting rules.
  \item \textbf{Freeze validity predicates.} Declare localization, distinguishability, persistence or resilience, and $V_{\mathrm{dom}}$ thresholds.
  \item \textbf{Declare boundary and denominator.} Define $\mathcal{B}$ and decide which costs, attempts, readouts, rejected events, and stabilization periods are included.
  \item \textbf{Run baseline models.} Compare against raw activity, raw crossings, first-passage counts, TPT/Markov-state rates, ordinary yield, or standard prediction models.
  \item \textbf{Estimate VST observables.} Report hard counts, probabilistic counts, finite-window rates, cost-normalized yields, uncertainty, and threshold sensitivity.
  \item \textbf{Assign claim level.} Label the result as proxy, task-level, or fully measured VST. Do not claim causal benefit unless a causal design supports it.
  \item \textbf{Release reproducibility materials.} Provide code, thresholds, state-region definitions, event lists, rejected-event counts, and denominator ledgers.
\end{enumerate}

\subsection{VSTF Pipeline}

The operational pipeline is:
\[
\begin{array}{c}
\mathrm{Observed\ data}\ Y(t)\\
\downarrow\\
\mathrm{Locked\ candidate\ event\ segmentation}\\
\downarrow\\
\mathrm{Localization\ valid?}\\
\downarrow\\
\mathrm{Distinguishable?}\\
\downarrow\\
\mathrm{Retention/resilience\ criterion\ met?}\\
\downarrow\\
\mathrm{Domain\ predicate\ admissible\ and\ passed?}\\
\downarrow\\
\mathrm{State\mbox{-}valid\ VST}\ (A_k=1)\\
\downarrow\\
\mathrm{Accounting\ complete?}\\
\downarrow\\
\mathrm{Cost\mbox{-}accounted\ VST,\ \VSTE{},\ \VSTJ{}}
\end{array}
\]
Failure before $A_k=1$ produces a rejected or unresolved candidate event. Failure after $A_k=1$ but before accounting completeness preserves the state-valid event while forbidding cost-normalized claims. Pending retention windows enter censoring analysis rather than the failure count.
When validity is observed under uncertainty, the operational decision $A_k$ and matched-reference validity $Z^{\mathrm{match}}_k$ are distinct; reporting should show both operational counts and any available adjudicated reference-standard validation, plus the matching rule that connects detected candidates to reference events.

\section{Four Levels of Evidence}

VSTF requires separation of evidence levels. Table~\ref{tab:evidence-levels} separates proxy, task-level, state-valid, and cost-accounted claims.

\begin{table}[h]
\centering
\caption{Evidence levels for valid transition claims.}
\label{tab:evidence-levels}
\begin{tabular}{p{0.22\linewidth}p{0.34\linewidth}p{0.34\linewidth}}
\toprule
Level & What is measured & What cannot be claimed \\
\midrule
Proxy VST & Public or incomplete data satisfy an approximate event rule. & Full physical or causal validation. \\
Task-level VST & A benchmark or protocol defines completed outcomes and energy per task. & Microscopic entropy production or retention unless measured. \\
State-valid VST & Localization, distinguishability, event-level retention or declared resilience, and admissible domain validity are measured under a locked protocol. & Energy-normalized efficiency or cross-system cost comparison without complete denominator accounting. \\
Cost-accounted VST & State-valid VST plus complete window-level denominator accounting $Q_{\mathcal{B}}^{\mathrm{acct}}[a,b]=1$. & Causal treatment benefit or generalization beyond the tested boundary without separate validation. \\
\bottomrule
\end{tabular}
\end{table}

\section{Illustrative Domain Mappings and Validation Templates}

\subsection{Computational Hardware}

For VLSI, edge-AI, stochastic devices, memory cells, or quantum readout systems, define state regions as accepted output classes, memory states, pointer states, or retained device states.

An event is valid only if:
\begin{enumerate}
  \item the output is localized in a declared class,
  \item distinguishability exceeds the readout threshold,
  \item retention persists over $\tau_\ast$,
  \item rejected attempts remain in the energy denominator,
  \item quality or correctness predicates are passed.
\end{enumerate}

At this layer, VSTF recovers the logic of a valid-output-per-power metric, previously referred to by the author as the Valid Irreversible Resolution Rate:
\[
  \VIRR = \frac{R_{\mathrm{valid}}}{P}.
\]
This quantity is used here only as an internal predecessor to the VSTF reporting grammar, not as a separately established community standard. The contribution is not arithmetic novelty when public benchmarks already report energy per task or full-system power \cite{mlperf}. The contribution is the explicit event boundary, the accepted-outcome denominator, and the refusal to count raw activity as an accepted retained outcome.

\noindent\textbf{Minimum confirmatory hardware protocol.}
\begin{enumerate}
  \item Freeze task definition, input distribution, correctness criterion, retention window, and readout method.
  \item Count all candidate attempts, not only successful outputs.
  \item Measure full-system energy at the declared boundary, including host, memory, conversion, calibration, readout, and failed attempts when inside $\mathcal{B}$.
  \item Report raw operations per watt, accepted outputs per watt, retained accepted outputs per watt, and error burden.
  \item Test whether VSTF changes ranking against a standard benchmark denominator.
\end{enumerate}

The hardware claim is falsified if accepted-output yield is not materially different from ordinary benchmark yield after full-boundary accounting, or if failures are already fully captured by the benchmark's native validity rules.

\subsection{Physiological Stability}

Two distinct ICU uses must not be conflated. In a \emph{patient-state VST}, $X_t$ is the latent physiological state of the patient and the prediction model estimates a patient transition. In a \emph{decision-state VST}, $X_t$ is the state of an alarm, triage, or clinical decision system. Both are legitimate, but a manuscript must declare which ontology is being evaluated.

In intensive-care time series, let
\[
  x(t)=[\mathrm{HR},\mathrm{MAP},\mathrm{RR},\mathrm{SpO}_2,\mathrm{Temp},\ldots]
\]
and define dynamic components:
\[
  P(t) = \mathrm{perturbation\ intensity},\quad
  A(t) = \mathrm{autocorrelation\ persistence},
\]
\[
  R(t) = \mathrm{relaxation\ proxy},\quad
  D_C(t)=\mathrm{coupling\ change}.
\]

For patient-state use, the physiological stability framework becomes a VSTF problem only when an elevated dynamic state is linked to a valid patient-level transition under locked rules:
\[
  \mathrm{dynamic\ change} \longrightarrow \mathrm{validated\ patient\ transition}.
\]
For decision-state use, the VST event is the accepted alarm or decision transition, not the patient's biological transition itself. Until patient-level calibration, raw-vital baselines, alarm burden, decision curves, and external evaluation are completed, PSI-like outputs should be treated as component-level instability features rather than clinical VST events.

A prediction-model output is not itself a patient-state transition. Predictive calibration and discrimination are necessary but do not establish patient benefit. A patient-state VST claim requires an independently meaningful clinical endpoint and, where benefit is claimed, an appropriate causal or interventional design.

A clinical VST claim requires a declared ontology--patient state or decision-system state--and must separately establish measurement validity, predictive performance, calibration, decision utility, and, where claimed, causal treatment benefit. Death, discharge, treatment escalation, treatment limitation, transfer, and loss of measurement are intercurrent events. They must be assigned a pre-specified estimand strategy such as treatment-policy, hypothetical, composite, while-on-treatment, or principal-stratum handling.

A minimal clinical validation panel should include calibration-in-the-large, calibration slope or curve, discrimination, Brier score, net benefit, alarm burden, and an explicit baseline such as NEWS2 or a raw-vital model \cite{news2,trwscore,physionet,harutyunyan2019}. Splits must be by patient, not prediction instance, and temporal generalization should be tested by training on earlier cohorts and evaluating on later cohorts when the data permit.

Reporting should follow TRIPOD+AI for prediction-model manuscripts; methodological quality, risk of bias, and applicability should be assessed with PROBAST+AI \cite{tripod,collins2024tripodai,moons2025probastai}. TRIPOD+AI is a reporting standard, not proof of model validity; PROBAST+AI is an appraisal framework, not a clinical-utility test. No reporting guideline or risk-of-bias tool substitutes for patient-relevant utility evaluation.

\noindent\textbf{Minimum confirmatory ICU protocol.}
\begin{enumerate}
  \item Freeze the clinical endpoint, prediction horizon, exclusion rules, preprocessing, and alarm threshold.
  \item Split development, calibration, and external evaluation cohorts by patient; additionally split by hospital, unit, or time period where possible.
  \item Compare against NEWS2, raw vital signs, and a standard time-series baseline.
  \item Report calibration, discrimination, Brier score, net benefit, alarm burden, false alarms per patient-day, and missed-event rate.
  \item Pre-specify how death, discharge, treatment escalation, treatment limitation, transfer, and measurement dropout enter the endpoint and denominator.
  \item Treat a PSI-like signal as a VST event only when it changes externally validated patient-level prediction or decision utility.
\end{enumerate}

The clinical VST claim is falsified if dynamic coupling features do not improve externally validated prediction or utility beyond simpler baselines.

\subsection{Pathological Tumor-State Reset}

For tumor systems, where wound-repair analogies and modern hallmarks motivate state-based descriptions of malignancy \cite{dvorak1986,hanahan2011,hanahan2022}, define a module vector
\[
  x(t)=
  [S_{\mathrm{ECM}},S_{\mathrm{hypoxia}},S_{\mathrm{proteostasis}},S_{\mathrm{alarm}},
  C_{\mathrm{junction}},C_{\mathrm{polarity}},C_{\mathrm{circadian}},C_{\mathrm{mitochondrial}},\ldots].
\]

Here \OSD{} is used only as a declared multivariate organism-stability-disruption score derived from pre-specified molecular modules. It is not assumed to be a universal cancer mechanism. A safer biological term is \emph{pathological tumor-state} unless a wound-repair signature is explicitly part of the module definition.

Let $x_{\mathrm{path}}$ be the baseline pathological tumor-state vector and $x_{\mathrm{ref}}$ a frozen low-\OSD{} or normal-like reference. Define
\[
\begin{aligned}
  D_{\mathrm{path}}(t)
  &=
  (x(t)-\mu_{\mathrm{path}})^\top
  \Sigma_{\mathrm{path}}^{-1}
  (x(t)-\mu_{\mathrm{path}}),\\
  D_{\mathrm{ref}}(t)
  &=
  (x(t)-\mu_{\mathrm{ref}})^\top
  \Sigma_{\mathrm{ref}}^{-1}
  (x(t)-\mu_{\mathrm{ref}}).
\end{aligned}
\]
\[
  Y(t)=D_{\mathrm{path}}(t)-D_{\mathrm{ref}}(t).
\]
Here $\mu_{\mathrm{path}}$, $\mu_{\mathrm{ref}}$, feature scaling, module definitions, and covariance estimates must be frozen from an independent training/reference cohort before confirmatory testing. The protocol must state whether $\Sigma_{\mathrm{path}}=\Sigma_{\mathrm{ref}}$ is a pooled covariance, whether one state-specific covariance is reused for both distances, or whether two separate covariance matrices are used. If $\Sigma_{\mathrm{path}}\neq\Sigma_{\mathrm{ref}}$, the primary discrimination statistic should usually be a log-likelihood contrast rather than a bare difference of squared Mahalanobis distances:
\[
  \Lambda(x)=\log p_{\mathrm{ref}}(x)-\log p_{\mathrm{path}}(x),
\]
and, under Gaussian reference and pathological distributions,
\[
\begin{aligned}
  \Lambda(x)
  &=
  -\frac{1}{2}(x-\mu_{\mathrm{ref}})^\top
  \Sigma_{\mathrm{ref}}^{-1}
  (x-\mu_{\mathrm{ref}})
  -\frac{1}{2}\log|\Sigma_{\mathrm{ref}}|\\
  &\quad
  +\frac{1}{2}(x-\mu_{\mathrm{path}})^\top
  \Sigma_{\mathrm{path}}^{-1}
  (x-\mu_{\mathrm{path}})
  +\frac{1}{2}\log|\Sigma_{\mathrm{path}}|\\
  &\quad
  +\log\frac{\pi_{\mathrm{ref}}}{\pi_{\mathrm{path}}}.
\end{aligned}
\]
If the number of features is not small relative to the training cohort, covariance inverses must be estimated with a pre-specified shrinkage, diagonal, low-rank, or regularized covariance protocol, for example
\[
  \widehat{\Sigma}_\lambda=(1-\lambda)\widehat{\Sigma}+\lambda\tau I,
  \qquad
  \tau=\frac{\mathrm{tr}(\widehat{\Sigma})}{p},
\]
after features have been placed on a commensurate scale.

A reset transition is valid only if the post-rechallenge state remains displaced away from $x_{\mathrm{path}}$ and toward $x_{\mathrm{ref}}$ after withdrawal:
\[
  \Delta_{\mathrm{path}}
  =
  D_{\mathrm{path}}(T4)-D_{\mathrm{path}}(T0)
  \geq \eta_{\mathrm{path}},
  \qquad
  \Delta_{\mathrm{ref}}
  =
  D_{\mathrm{ref}}(T0)-D_{\mathrm{ref}}(T4)
  \geq \eta_{\mathrm{ref}},
\]
with viability, target engagement, clone-selection controls, and locked module definitions. The scalar contrast $Y(t)=D_{\mathrm{path}}(t)-D_{\mathrm{ref}}(t)$ may be reported as a secondary summary, but it is not sufficient by itself because it can improve through movement in only one direction.

Acute cytotoxicity is not a valid reset by itself, and ``closer to a normal-like reference'' is evidence of molecular state shift, not by itself evidence of biological restoration. A substantive tumor-state-reset claim must additionally require a pre-specified independent functional endpoint in viable surviving populations, with clone selection, treatment-induced cytotoxicity, batch effects, and proliferation-rate changes explicitly controlled. If lineage-aware tracking is unavailable, the safer claim is a population-state shift rather than reset of the same surviving lineage. Examples include clonogenic recovery, invasion, tumor-initiation potential, treatment sensitivity, differentiation phenotype, or another locked assay appropriate to the model. The primary confirmatory analysis should estimate a treatment-by-time effect with biological-replicate or clone-level random effects, rather than relying only on the sign of a single post-treatment difference. This is consistent with modern work on therapy-induced cancer-cell state transitions and persister lineages \cite{oren2021}.
For tumor-marker or biomarker-style reporting, REMARK remains the appropriate historical reporting antecedent \cite{remark}.

\noindent\textbf{Minimum confirmatory cancer protocol.}
\begin{enumerate}
  \item Freeze module definitions, normalization, reference cohorts, covariance/shrinkage method, and reset threshold before testing.
  \item Include vehicle, cytotoxic-positive, pathway-specific, withdrawal, and rechallenge conditions.
  \item Distinguish cell killing from surviving-cell state reset by measuring viable surviving populations.
  \item Control for clone selection, lineage effects, batch effects, and proliferation-rate artifacts.
  \item Include an independent functional endpoint for any substantive tumor-state-reset claim.
  \item Use a longitudinal model with treatment, time, treatment-by-time interaction, and replicate or clone effects.
\end{enumerate}

The reset claim is falsified if the apparent improvement disappears after withdrawal, rechallenge, viable-survivor analysis, or clone-selection control.

\subsection{Neurorehabilitation}

Let
\[
  x(t)=[U(t),E(t),M(t),Y(t),S_f(t),A_u(t),B_p(t)]
\]
represent voluntary intent, stimulation, EMG, movement, sensory feedback, autonomic state, and pain/spasticity/fatigue. The latent intent variable $U(t)$ is not directly observed; it must be connected to an independent measurement channel $U^{\mathrm{meas}}(t)$ through a declared measurement model or adjudication rule before it can support a VST claim. That measurement channel must have pre-specified latency, false-positive, and artifact-rejection characteristics; otherwise intent can become an unvalidated post hoc label for movement.

A motor event is provisionally valid only if movement depends on intent under stimulation:
\[
  \Pr(Y=1\mid U=1,E=1)>\Pr(Y=1\mid U=0,E=1),
\]
or, alternatively, conditional dependence may be quantified by
\[
  I(U;Y\mid E,P_{\mathrm{body}},T_{\mathrm{session}})>0.
\]
These inequalities establish conditional dependence, not by themselves causal voluntary control. A confirmatory design should randomize instructed-attempt or cue condition and stimulation or sham condition where ethically and practically possible, measure realized voluntary intent independently from the movement endpoint, use artifact masking, pre-specified lag windows, negative temporal controls, and clustered inference by participant and session. A compact model is
\[
  \mathrm{logit}\,\Pr(Y=1)
  =
  \beta_0+\beta_U U+\beta_E E+\beta_{UE}(U\times E)+\beta_Z Z,
\]
where $Z$ contains posture, cue timing, prior stimulation, fatigue, and artifact controls.
The interaction coefficient $\beta_{UE}$ is a log-odds-scale parameter; it should not automatically be treated as the primary clinical effect. A more interpretable pre-specified estimand is
\[
  \Delta_U=
  \Pr(Y=1\mid U=1,E=1,Z)-\Pr(Y=1\mid U=0,E=1,Z),
\]
with a minimum clinically meaningful margin $\Delta_U\geq\eta_U$ declared before testing. The intent variable $U$ must be measured independently of the movement outcome used to define $Y$; otherwise intent-outcome coupling can become circular.
When independent intent measurement is weak, the primary causal estimand should be defined on the randomized instructed-attempt or cue condition rather than directly on latent $U$:
\[
  \Delta_{\mathrm{cue}}=
  \Pr(Y=1\mid Z_{\mathrm{intent}}=1,E=1,Z)
  -
  \Pr(Y=1\mid Z_{\mathrm{intent}}=0,E=1,Z),
\]
where $Z_{\mathrm{intent}}$ is the randomized instruction or cue and $Z$ contains pre-specified posture, timing, fatigue, artifact, and session controls. In that design, $U$ is a mediator or mechanistic measurement target; it is not the sole basis for the causal claim unless separately validated.
As a negative temporal-control test, physiologically impossible alignments should fail, for example
\[
  I(U_{t+\Delta};Y_t\mid E_t)\approx 0
\]
for future intent predicting past movement, or under a pre-specified time-shift/permutation null.

Retention requires:
\[
  \Delta_{\mathrm{ret}}=
  \Pr(Y=1\mid U=1,E=0,\mathrm{post})
  -
  \Pr(Y=1\mid U=1,E=0,\mathrm{baseline})
  \geq \eta_{\mathrm{ret}},
\]
with uncertainty intervals reported at the participant or session-cluster level.

Thus a stimulation-evoked contraction is not a valid functional transition unless it is localized, distinguishable from artifact or reflex, stable or retained over an operational window, and coupled to voluntary intent. Off-stimulation post-training performance should be reported as a separate retention endpoint. Human spinal-cord stimulation studies and work on metastable neural dynamics provide useful precedents because they explicitly separate stimulation-supported movement, voluntary control, proprioceptive constraints, retained post-training function, and circuit-state dynamics \cite{brinkman2022,wagner2018,formento2018}.

\noindent\textbf{Minimum confirmatory neurorehabilitation protocol.}
\begin{enumerate}
  \item Randomize instructed-attempt or cue condition and stimulation or sham condition where ethically and practically possible.
  \item Use the randomized instructed-attempt or cue condition as the primary causal contrast when latent intent is uncertain.
  \item Measure voluntary intent independently of the movement endpoint and predefine a minimum effect-size margin.
  \item Predefine lag windows between intent, stimulation, EMG, and movement.
  \item Include a time-shift or permutation negative control that should destroy intent-outcome alignment.
  \item Mask or model stimulation artifacts, reflex responses, posture, fatigue, pain, spasticity, and session effects.
  \item Report stimulation-on performance separately from stimulation-off post-training retention.
  \item Use participant- and session-clustered inference rather than treating repeated movement attempts as independent.
\end{enumerate}

The voluntary-control claim is falsified if movement is equally explained by stimulation timing, artifact, posture, reflex, or cue structure without independent contribution from voluntary intent.

\section{General Falsification Criteria}

\begin{criterion}[Baseline sufficiency]
If a simpler baseline model explains the outcome with equal or better performance under the same validation procedure, the VST-specific claim is not supported.
\end{criterion}

\begin{criterion}[Antecedent sufficiency]
If a standard TPT, first-passage, Markov-state, reliability-yield, or clinical-validation model captures the claimed result without the VSTF acceptance layer changing inference, ranking, or decision utility, then the incremental VSTF claim is not supported.
\end{criterion}

\begin{criterion}[Event-rule failure]
If localization, distinguishability, persistence or resilience, accounting quality, or the domain predicate cannot be measured or fails under a locked protocol, only a proxy claim is allowed.
\end{criterion}

\begin{criterion}[Post-hoc threshold failure]
If the operating threshold is selected after outcome inspection and not revalidated prospectively, the result is exploratory.
\end{criterion}

\begin{criterion}[No-retention failure]
If the state moves during perturbation but returns to the original basin after withdrawal or rechallenge, the event is activity or response, not a valid stabilized transition.
\end{criterion}

\begin{criterion}[Boundary failure]
If major energy, dose, rejected attempts, readout, or stabilization costs are excluded from the declared denominator, energy-normalized claims are invalid for cross-system comparison.
\end{criterion}

\begin{criterion}[Accounting-validity separation]
If a candidate event passes localization, distinguishability, persistence, and domain validity but lacks a complete cost ledger, it may be reported as a state-valid event but cannot support \VSTE{}, cost-yield, or cross-system efficiency claims.
\end{criterion}

\section{Reporting Standard}

Every VSTF study should report:

\begin{enumerate}
  \item state variables and preprocessing rules,
  \item operational state regions $C_i$,
  \item whether $C_i$ were fixed a priori or estimated from a training/reference cohort,
  \item observation model $P_M(Y\mid X)$ or a justified measurement-error approximation,
  \item candidate-event segmentation: origin, departure, commitment, end, hysteresis, refractory interval, and double-counting rule,
  \item event index timestamp and right-censoring rule for finite windows,
  \item acquisition window, maturity/follow-up horizon, and whether the primary estimand is mature-window, complete-case, or censoring-aware,
  \item measurement map and distinguishability threshold or error criterion,
  \item latent process model $(p_0,K_\theta,P_M)$ or calibrated classifier-localization procedure,
  \item persistence window $\tau_\ast$ and first-exit or return-time rule,
  \item domain validity predicate,
  \item admissibility justification for $V_{\mathrm{dom}}$,
  \item event-granularity comparability or sensitivity analysis,
  \item system boundary and energy or perturbation ledger,
  \item \VSTE{} ledger alignment: calendar-window or event-complete,
  \item accounting-quality flag $Q^{\mathrm{acct}}$,
  \item window-level denominator-completeness flag $Q_{\mathcal{B}}^{\mathrm{acct}}[a,b]$,
  \item accepted and rejected candidate counts,
  \item baseline models and negative controls,
  \item uncertainty analysis and dependence corrections,
  \item sensitivity to $\epsilon$, $\alpha$, $\delta$, $\tau_\ast$, segmentation, and domain thresholds,
  \item exact code or command-level reproducibility path,
  \item explicit claim level: proxy, task-level, state-valid, or cost-accounted VST.
\end{enumerate}

\section{Preregisterable Hypotheses}

\begin{hypothesis}[Ranking reversal under full cost boundaries]
In a benchmark population of systems with matched raw activity efficiency, the ranking induced by $\widehat{\VSTE}$ will differ from the ranking induced by raw activity per watt after rejected attempts, retention failures, readout, and stabilization overhead are included.

\noindent\textbf{Primary statistic.} For $m$ systems, define the fraction of discordant pairs
\[
  D=
  \frac{
  \sum_{i<j}
  \1\{\mathrm{sgn}(R_i-R_j)\neq \mathrm{sgn}(V_i-V_j)\}
  }{\binom{m}{2}},
\]
where $R_i$ is raw activity efficiency and $V_i$ is $\widehat{\VSTE}$ for system $i$. Ties must be handled by a locked rule.

The two-system HMM benchmark in this manuscript is a didactic A/B demonstration of possible ranking reversal, not a full test of this hypothesis. A confirmatory H1 benchmark should use many systems, for example 30--50 synthetic or empirical systems sampled over a declared parameter space, and should resample systems rather than treating pair indicators as independent observations.

\noindent\textbf{Null hypothesis.} $H_0:D\leq D_{\min}$, where $D_{\min}$ is the minimum scientifically relevant discordant-pair fraction.

\noindent\textbf{Falsification rule.} If $D$ does not exceed $D_{\min}$ with pre-specified uncertainty control, or if discordant ranking changes are not reproducible in an external benchmark set, the VSTF ranking-reversal claim is not supported for that domain.
\end{hypothesis}

\begin{hypothesis}[Coupling over amplitude]
In complex biological time series, models containing pre-specified coupling-change variables will improve externally validated prediction of fully measured VST events over amplitude-only models.

\noindent\textbf{Primary endpoint.} The primary endpoint should be declared in advance, for example $\Delta$Brier score for probabilistic prediction or an integrated net benefit
\[
  \Delta NB_{\mathrm{integrated}}
  =
  \int_{\theta_1}^{\theta_2}
  w(\theta)\,[NB_{\mathrm{coupling}}(\theta)-NB_{\mathrm{baseline}}(\theta)]\,d\theta,
\]
where $\theta_1$, $\theta_2$, and $w(\theta)$ are locked before evaluation. Calibration, discrimination, and alarm burden should be secondary endpoints unless declared primary.

\noindent\textbf{Null hypothesis.} Coupling variables do not improve the pre-specified primary endpoint beyond an amplitude-only baseline in external validation.

\noindent\textbf{Falsification rule.} If improvement is absent after external validation, uncertainty estimation, and multiplicity control for secondary endpoints, the coupling claim is rejected for that setting.
\end{hypothesis}

\begin{hypothesis}[Retention attrition after acute response]
In perturbation experiments, acute response does not imply durable reset. Among acute candidate responses, define
\[
  p_{\mathrm{ret}}=\Pr(\mathrm{retained\ reset}\mid \mathrm{acute\ response}).
\]
Equivalently, define the acute-response attrition probability
\[
  a_{\mathrm{ret}}=1-p_{\mathrm{ret}}.
\]
The nested inequality $N_{\mathrm{retained}}\leq N_{\mathrm{acute}}$ is a definitional consequence of the event hierarchy; the empirical hypothesis concerns whether attrition is scientifically large enough to change the interpretation of the perturbation.

\noindent\textbf{Null hypothesis.} $H_0:a_{\mathrm{ret}}\leq\rho_\ast$ against $H_1:a_{\mathrm{ret}}>\rho_\ast$, where $\rho_\ast$ is a pre-specified maximum acceptable attrition margin. Alternatively, compare two protocols by testing $p_{\mathrm{ret}}^{(A)}=p_{\mathrm{ret}}^{(B)}$ or $a_{\mathrm{ret}}^{(A)}=a_{\mathrm{ret}}^{(B)}$.

\noindent\textbf{Falsification rule.} If acute responses are retained after withdrawal and rechallenge with attrition below the pre-specified margin, or if no protocol-level difference in $p_{\mathrm{ret}}$ is observed where predicted, the VSTF reset-vs-response claim is unsupported for that protocol.
\end{hypothesis}

\begin{conjecture}[Decision-loop bottleneck]
In computational architectures dominated by measurement, branching, synchronization, readout, or feedback, reducing pre-specified decision-loop closures may improve accepted outcome yield more than increasing raw internal activity at fixed decision overhead. Let
\[
  C_{\mathrm{loop}}
\]
denote a locked count of branch, readout, synchronization, or feedback closures for a declared workload. This conjecture becomes a preregisterable hypothesis only when an intervention changes $C_{\mathrm{loop}}$ while holding architecture, memory traffic, throughput target, and error tolerance sufficiently fixed.

\noindent\textbf{Null hypothesis for a future test.} Accepted outcome yield is explained fully by raw internal activity, throughput, error rate, memory traffic, and full-system energy, with no additional contribution from $C_{\mathrm{loop}}$.

\noindent\textbf{Falsification rule.} If decision-loop reductions do not improve $\widehat{\VSTE}$ after controlling for throughput, error rate, memory traffic, and full-system power, the bottleneck claim is not supported.
\end{conjecture}

\section{Limitations}

\begin{limitation}[Coarse-graining dependence]
VST observables depend on the chosen state partition. This is not a defect if the partition is declared, justified, and stress-tested. It is a defect if partitions are tuned after outcome inspection.
\end{limitation}

\begin{limitation}[Domain specificity]
The validity predicate is domain-specific. A universal mathematical skeleton does not imply a universal biological or physical mechanism.
\end{limitation}

\begin{limitation}[No automatic causality]
Retrospective association between a score and an endpoint does not establish that the score causes, controls, or can prevent the transition.
\end{limitation}

\begin{limitation}[No scalar absolutism]
The framework does not require a single universal scalar. In many domains, the correct output is a tuple: rate, energy-normalized yield, retention, false-event burden, and component attribution.
\end{limitation}

\section{Ethical Scope of Future Validation}

This manuscript is a theoretical and methodological synthesis and uses no new human or animal data. Therefore, institutional review-board or animal-care approval is not required for the manuscript itself.

Future ICU, oncology, neurorehabilitation, or animal-tumor validation studies would require their own ethical review. Human studies should specify informed consent or waiver, privacy protection for identifiable data, adverse-event monitoring, and fairness analysis when prediction models may affect clinical decisions. Animal studies should follow ARRIVE 2.0 reporting principles and justify sample size, randomization, blinding, exclusions, and humane endpoints \cite{arrive2020}. In all clinical applications, measurement validity, predictive validity, clinical utility, and causal treatment benefit must be reported as separate claims.

\section{Claim Audit and Reviewer-Safe Interpretation}

The framework is easiest to overstate in four places. The following interpretations are therefore part of the formal manuscript rather than informal caveats.

\subsection{Novelty Claim}

VSTF does not claim to discover transition paths, state basins, event rates, or first-exit processes. Its novelty is the cross-domain acceptance grammar that determines when a dynamically detected event becomes a countable outcome under locked validity and cost rules. If a domain already has an equivalent acceptance grammar, VSTF should be described as a translation or unification, not as a new mechanism.

\subsection{Thermodynamic Claim}

VSTF does not assign a universal Landauer cost to every biological, clinical, or computational event. Landauer-type bounds apply only to logically irreversible information operations under the relevant assumptions. For other systems, energy, entropy, dose, or risk burden are denominators for accountable comparison, not universal physical lower bounds.

Thermodynamic work/free-energy relations are treated as conditional physical specializations. The general VSTF metric is an engineering or methodological ratio of accepted retained outcomes to a declared denominator. It remains meaningful even when no microscopic thermodynamic lower bound is asserted.

\subsection{Clinical Claim}

VSTF does not allow a dynamic instability score to be called a clinical event. A clinical VST requires external validation, calibration, utility assessment, comparison with simpler baselines, and an explicit estimand strategy for intercurrent events such as death, discharge, or treatment escalation. Measurement validity, prediction validity, decision utility, and treatment benefit are distinct layers.

\subsection{Biological Mechanism Claim}

VSTF does not assert that cancer reset, physiological stabilization, and motor recovery share one biological mechanism. It asserts only that all three can be evaluated using the same event-level grammar: state localization, distinguishability, retention, domain validity, and accountable denominator. Terms such as repair-like state, tumor-state reset, or organism-stability disruption must be operationally defined in each biological study.

\subsection{What Would Make VSTF Scientifically Unnecessary}

VSTF would add little if, across representative domains, accepted-transition counts never differ from raw activity, first-passage counts, standard yield metrics, or ordinary validated prediction models. The framework earns its place only when the acceptance layer changes ranking, interpretation, rejection of resolved committed events that fail downstream VST criteria, or protocol design.

\section{Discussion}

The central move of VSTF is simple: change the counted object. Many existing frameworks ask how much energy is used, how much entropy is produced, how much information is transmitted, how much activity occurs, or how large a score becomes. VSTF asks whether the system produced a valid stabilized transition.

This reframing is valuable because it separates activity from outcome. A dissipative process may produce no accepted transition. A high-throughput device may fail retention. A tumor perturbation may kill cells without resetting surviving state dynamics. A neuromodulation protocol may evoke movement without voluntary control. A physiological score may detect weak instability without clinical utility.

The framework is strongest where the four state-validity predicates and the accounting-quality flag can all be measured under a locked protocol. It is weakest when only proxy features exist. The intended use is therefore not rhetorical unification but disciplined experimental design.

\section{Conclusion}

The Valid State Transition Framework formalizes a cross-domain measurement principle: count stabilized, distinguishable, valid transitions rather than raw activity. The framework is compatible with thermodynamics, stochastic trajectories, critical-transition theory, clinical model validation, and systems biology, but it does not replace them. Its contribution is operational, auditable, and empirically contestable through domain-specific falsifiable hypotheses.

The practical question becomes:
\[
  \text{How many valid stabilized state transitions does the system produce per unit cost?}
\]

This question can be applied to hardware, physiological monitoring, cancer-state reset experiments, neurorehabilitation, and other systems where transient activity and durable outcome are easily confused. The framework should advance only where locked protocols, baseline comparisons, negative controls, and reproducible ledgers show that it adds information beyond simpler explanations.

\section*{Declarations}

\textbf{Ethics approval and consent to participate.} This manuscript is a theoretical and methodological synthesis and uses no new human participants, human tissue, animal experiments, or identifiable personal data. Institutional review-board approval, animal-care approval, and participant consent are therefore not applicable to this manuscript.

\textbf{Consent for publication.} Not applicable; the manuscript contains no identifiable individual-level human data.

\begin{sloppypar}
\textbf{Data and code availability.} This manuscript is a theoretical and methodological synthesis with a synthetic HMM demonstration and public real-data component audits. The simulation scripts, CSV summaries, event-level audit sample, multi-dataset real-data acceptance script, component-validation script, household-power event ledger, Lalonde treatment-effect ledger, full real-calculation ledger, reproducibility manifest, requirements file, workflow figure, benchmark figure, and many-system rank figure are provided in the companion VSTF package. The public repository is the \href{https://github.com/MartinPetrasek123/R_Universe_Evidence_Audited/tree/main/vstf}{VSTF reproducibility package}; final submission should cite an immutable release DOI or exact commit hash. The real-data audit uses public datasets linked directly in Tables~\ref{tab:real-static-datasets} and \ref{tab:real-component-datasets}: WDBC, Ionosphere, Sonar, Cleveland Heart Disease, Haberman Survival, UCI Individual Household Electric Power Consumption, and the Lalonde NSW treatment dataset. Domain-specific empirical claims require separate reproducibility packages, locked datasets, code, and analysis logs.
\end{sloppypar}

\textbf{Materials availability.} No biological materials, clinical specimens, proprietary devices, or physical samples are associated with this manuscript.

\textbf{Funding.} No external funding is declared for this manuscript.

\textbf{Competing interests.} The author declares no competing interests.

\textbf{Author contribution.} M. Petrasek conceived the framework, integrated prior theoretical and applied components, and wrote the manuscript.

\textbf{Acknowledgements.} No separate acknowledgements are declared.

\begin{thebibliography}{99}

\bibitem{street1993wdbc}
W. N. Street, W. H. Wolberg, and O. L. Mangasarian.
Nuclear feature extraction for breast tumor diagnosis.
In \emph{Biomedical Image Processing and Biomedical Visualization}, volume 1905, pages 861--870, 1993.
doi:10.1117/12.148698.

\bibitem{uci_wdbc}
UCI Machine Learning Repository.
\emph{Breast Cancer Wisconsin (Diagnostic) Data Set}.
University of California, Irvine.
\url{https://archive.ics.uci.edu/ml/machine-learning-databases/breast-cancer-wisconsin/wdbc.data}.
Accessed 19 September 2026.

\bibitem{landauer1961}
R. Landauer.
Irreversibility and heat generation in the computing process.
\emph{IBM Journal of Research and Development}, 5(3):183--191, 1961.
doi:10.1147/rd.53.0183.

\bibitem{bennett2003}
C. H. Bennett.
Notes on Landauer's principle, reversible computation, and Maxwell's demon.
\emph{Studies in History and Philosophy of Modern Physics}, 34(3):501--510, 2003.
doi:10.1016/S1355-2198(03)00039-X.

\bibitem{berut2012}
A. Berut, A. Arakelyan, A. Petrosyan, S. Ciliberto, R. Dillenschneider, and E. Lutz.
Experimental verification of Landauer's principle linking information and thermodynamics.
\emph{Nature}, 483:187--189, 2012.
doi:10.1038/nature10872.

\bibitem{seifert2005}
U. Seifert.
Entropy production along a stochastic trajectory and an integral fluctuation theorem.
\emph{Physical Review Letters}, 95:040602, 2005.
doi:10.1103/PhysRevLett.95.040602.

\bibitem{seifert2012}
U. Seifert.
Stochastic thermodynamics, fluctuation theorems and molecular machines.
\emph{Reports on Progress in Physics}, 75:126001, 2012.
doi:10.1088/0034-4885/75/12/126001.

\bibitem{barato2015}
A. C. Barato and U. Seifert.
Thermodynamic uncertainty relation for biomolecular processes.
\emph{Physical Review Letters}, 114:158101, 2015.
doi:10.1103/PhysRevLett.114.158101.

\bibitem{jarzynski1997}
C. Jarzynski.
Nonequilibrium equality for free energy differences.
\emph{Physical Review Letters}, 78(14):2690--2693, 1997.
doi:10.1103/PhysRevLett.78.2690.

\bibitem{sagawa2010}
T. Sagawa and M. Ueda.
Generalized Jarzynski equality under nonequilibrium feedback control.
\emph{Physical Review Letters}, 104:090602, 2010.
doi:10.1103/PhysRevLett.104.090602.

\bibitem{kramers1940}
H. A. Kramers.
Brownian motion in a field of force and the diffusion model of chemical reactions.
\emph{Physica}, 7(4):284--304, 1940.
doi:10.1016/S0031-8914(40)90098-2.

\bibitem{e2006}
W. E and E. Vanden-Eijnden.
Towards a theory of transition paths.
\emph{Journal of Statistical Physics}, 123:503--523, 2006.
doi:10.1007/s10955-005-9003-9.

\bibitem{metzner2009}
P. Metzner, C. Schutte, and E. Vanden-Eijnden.
Transition path theory for Markov jump processes.
\emph{Multiscale Modeling \& Simulation}, 7(3):1192--1219, 2009.
doi:10.1137/070699500.

\bibitem{yu2010hsmm}
S.-Z. Yu.
Hidden semi-Markov models.
\emph{Artificial Intelligence}, 174(2):215--243, 2010.
doi:10.1016/j.artint.2009.11.011.

\bibitem{maler2004}
O. Maler and D. Nickovic.
Monitoring temporal properties of continuous signals.
In \emph{Formal Techniques, Modelling and Analysis of Timed and Fault-Tolerant Systems}, pages 152--166. Springer, 2004.
doi:10.1007/978-3-540-30206-3\_12.

\bibitem{fainekos2009}
G. E. Fainekos and G. J. Pappas.
Robustness of temporal logic specifications for continuous-time signals.
\emph{Theoretical Computer Science}, 410(42):4262--4291, 2009.
doi:10.1016/j.tcs.2009.06.021.

\bibitem{reinbacher2014rv}
T. Reinbacher, M. F{\"u}gger, and J. Brauer.
Runtime verification of embedded real-time systems.
\emph{Formal Methods in System Design}, 44:203--239, 2014.
doi:10.1007/s10703-013-0199-z.

\bibitem{scheffer2009}
M. Scheffer, J. Bascompte, W. A. Brock, V. Brovkin, S. R. Carpenter, V. Dakos, H. Held,
E. H. van Nes, M. Rietkerk, and G. Sugihara.
Early-warning signals for critical transitions.
\emph{Nature}, 461:53--59, 2009.
doi:10.1038/nature08227.

\bibitem{scheffer2018}
M. Scheffer, J. E. Bolhuis, D. Borsboom, T. G. Buchman, S. M. W. Gijzel, D. Goulson,
J. E. Kammenga, B. Kemp, I. A. van de Leemput, S. Levin, C. M. Martin, R. J. F. Melis,
E. H. van Nes, L. M. Romero, and M. G. M. Olde Rikkert.
Quantifying resilience of humans and other animals.
\emph{Proceedings of the National Academy of Sciences}, 115(47):11883--11890, 2018.
doi:10.1073/pnas.1810630115.

\bibitem{dvorak1986}
H. F. Dvorak.
Tumors: wounds that do not heal. Similarities between tumor stroma generation and wound healing.
\emph{New England Journal of Medicine}, 315(26):1650--1659, 1986.
doi:10.1056/NEJM198612253152606.

\bibitem{hanahan2011}
D. Hanahan and R. A. Weinberg.
Hallmarks of cancer: the next generation.
\emph{Cell}, 144(5):646--674, 2011.
doi:10.1016/j.cell.2011.02.013.

\bibitem{hanahan2022}
D. Hanahan.
Hallmarks of cancer: new dimensions.
\emph{Cancer Discovery}, 12(1):31--46, 2022.
doi:10.1158/2159-8290.CD-21-1059.

\bibitem{oren2021}
Y. Oren, M. Tsabar, M. S. Cuoco, L. Amir-Zilberstein, H. F. Cabanos, J.-C. Hutter,
B. Hu, and colleagues.
Cycling cancer persister cells arise from lineages with distinct programs.
\emph{Nature}, 596:576--582, 2021.
doi:10.1038/s41586-021-03796-6.

\bibitem{brinkman2022}
B. A. W. Brinkman, H. Yan, A. Maffei, I. M. Park, A. Fontanini, J. Wang, and G. La Camera.
Metastable dynamics of neural circuits and networks.
\emph{Applied Physics Reviews}, 9(1):011313, 2022.
doi:10.1063/5.0062603.

\bibitem{wagner2018}
F. B. Wagner, J.-B. Mignardot, C. G. Le Goff-Mignardot, R. Demesmaeker, S. Komi,
and colleagues.
Targeted neurotechnology restores walking in humans with spinal cord injury.
\emph{Nature}, 563:65--71, 2018.
doi:10.1038/s41586-018-0649-2.

\bibitem{formento2018}
E. Formento, K. Minassian, F. B. Wagner, J.-B. Mignardot, C. G. Le Goff-Mignardot,
A. Rowald, and colleagues.
Electrical spinal cord stimulation must preserve proprioception to enable locomotion in humans with spinal cord injury.
\emph{Nature Neuroscience}, 21:1728--1741, 2018.
doi:10.1038/s41593-018-0262-6.

\bibitem{sturmberg2014}
J. P. Sturmberg, C. M. Martin, and D. A. Katerndahl.
Systems and complexity thinking in the general practice literature: an integrative, historical narrative review.
\emph{Annals of Family Medicine}, 12(1):66--74, 2014.
doi:10.1370/afm.1593.

\bibitem{news2}
Royal College of Physicians.
\emph{National Early Warning Score (NEWS) 2: Standardising the assessment of acute-illness severity in the NHS}.
Updated report of a working party. London: Royal College of Physicians, 2017.

\bibitem{trwscore}
T. Desautels, J. Calvert, J. Hoffman, M. Jay, Y. Kerem, L. Shieh, D. Shimabukuro, U. Chettipally, M. D. Feldman, C. Barton, D. J. Wales, and R. Das.
Prediction of sepsis in the intensive care unit with minimal electronic health record data: a machine learning approach.
\emph{JMIR Medical Informatics}, 4(3):e28, 2016.
doi:10.2196/medinform.5909.

\bibitem{physionet}
A. L. Goldberger, L. A. N. Amaral, L. Glass, J. M. Hausdorff, P. C. Ivanov, R. G. Mark,
J. E. Mietus, G. B. Moody, C.-K. Peng, and H. E. Stanley.
PhysioBank, PhysioToolkit, and PhysioNet: components of a new research resource for complex physiologic signals.
\emph{Circulation}, 101(23):e215--e220, 2000.
doi:10.1161/01.CIR.101.23.e215.

\bibitem{harutyunyan2019}
H. Harutyunyan, H. Khachatrian, D. C. Kale, G. Ver Steeg, and A. Galstyan.
Multitask learning and benchmarking with clinical time series data.
\emph{Scientific Data}, 6:96, 2019.
doi:10.1038/s41597-019-0103-9.

\bibitem{prentice1989}
R. L. Prentice.
Surrogate endpoints in clinical trials: definition and operational criteria.
\emph{Statistics in Medicine}, 8(4):431--440, 1989.
doi:10.1002/sim.4780080407.

\bibitem{ich2019e9r1}
International Council for Harmonisation of Technical Requirements for Pharmaceuticals for Human Use.
\emph{ICH E9(R1): Addendum on estimands and sensitivity analysis in clinical trials to the guideline on statistical principles for clinical trials}.
ICH, 2019.

\bibitem{mlperf}
V. J. Reddi et al.
MLPerf Inference Benchmark.
In \emph{Proceedings of the ACM/IEEE International Symposium on Computer Architecture}, pages 446--459, 2020.
doi:10.1109/ISCA45697.2020.00045.

\bibitem{phillips2014}
S. D. Phillips and M. Krystek.
Assessment of conformity, decision rules and risk analysis.
\emph{Technisches Messen}, 81(5):237--245, 2014.
doi:10.1515/teme-2014-1007.

\bibitem{chow1970reject}
C. K. Chow.
On optimum recognition error and reject tradeoff.
\emph{IEEE Transactions on Information Theory}, 16(1):41--46, 1970.
doi:10.1109/TIT.1970.1054406.

\bibitem{geifman2017selective}
Y. Geifman and R. El-Yaniv.
Selective classification for deep neural networks.
\emph{Advances in Neural Information Processing Systems Workshop}, 2017.
arXiv:1705.08500.

\bibitem{jcgm106}
Joint Committee for Guides in Metrology.
\emph{JCGM 106:2012: Evaluation of measurement data -- The role of measurement uncertainty in conformity assessment}.
JCGM, 2012.
doi:10.59161/JCGM106-2012.

\bibitem{remark}
L. M. McShane, D. G. Altman, W. Sauerbrei, S. E. Taube, M. Gion, and G. M. Clark.
Reporting recommendations for tumor marker prognostic studies.
\emph{Journal of Clinical Oncology}, 23(36):9067--9072, 2005.
doi:10.1200/JCO.2004.01.0454.

\bibitem{tripod}
G. S. Collins, J. B. Reitsma, D. G. Altman, and K. G. M. Moons.
Transparent reporting of a multivariable prediction model for individual prognosis or diagnosis: the TRIPOD statement.
\emph{Annals of Internal Medicine}, 162(1):55--63, 2015.
doi:10.7326/M14-0697.

\bibitem{collins2024tripodai}
G. S. Collins, K. G. M. Moons, P. Dhiman, R. D. Riley, A. L. Beam, B. Van Calster,
M. Ghassemi, X. Liu, J. B. Reitsma, M. van Smeden, and colleagues.
TRIPOD+AI statement: updated guidance for reporting clinical prediction models that use regression or machine learning methods.
\emph{BMJ}, 385:e078378, 2024.
doi:10.1136/bmj-2023-078378.

\bibitem{moons2025probastai}
K. G. M. Moons, J. A. A. Damen, T. Kaul, L. Hooft, C. Andaur Navarro, P. Dhiman,
and colleagues.
PROBAST+AI: an updated quality, risk of bias, and applicability assessment tool for prediction models using regression or artificial intelligence methods.
\emph{BMJ}, 388:e082505, 2025.
doi:10.1136/bmj-2024-082505.

\bibitem{arrive2020}
N. Percie du Sert, V. Hurst, A. Ahluwalia, S. Alam, M. T. Avey, M. Baker,
W. J. Browne, and colleagues.
The ARRIVE guidelines 2.0: updated guidelines for reporting animal research.
\emph{PLOS Biology}, 18(7):e3000410, 2020.
doi:10.1371/journal.pbio.3000410.

\end{thebibliography}

\end{document}
